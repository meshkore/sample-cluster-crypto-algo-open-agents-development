## Iteration 70 — proposal

**Module:** POLICY

**Claim:** On the corrected 100-asset, dollar-turnover-selected book, the forward drawdown breach is driven by aggregate long exposure in a correlated market rather than by single-position tail losses; cutting risk_per_trade and maximum_position_fraction so peak simultaneous deployed capital falls to roughly 50-60% of equity (holding maximum_concurrent_assets and all detector/module rule trees at incumbent) will bring forward max_drawdown below the mandate's ~30%-from-peak floor while keeping trade count w

**Killed by:** Refuted if forward max_drawdown still reaches >=30% from the running peak (mandate breaches again) despite the lower exposure ceiling, or if the fit fails to clear the POLICY gate because return/trade count collapses. Either outcome means the drawdown comes from per-trade tail losses under the widened book, not aggregate exposure, and the next POLICY iteration should move stop_loss_pct/take_profit_pct/maximum_holding_days instead of sizing caps.

H-L068U confirms all 37 prior forward runs, including H-L063's -20.02% refutation of 'moving POLICY improves score without breaching drawdown,' were measured on the defective ORDER-BY-first_seen 55-symbol alphabetical set. That refutation is void on this book, so re-calibrating POLICY is the first real measurement here, not a repeat of a dead direction. The diagnosis states nothing has been measured under this scope and that POLICY is the only lever that can move the drawdown the mandate rejects; incumbent_forward_return is null, so the incumbent's sizing is an artifact of a narrower, more volatile low-cap pool. Turnover selection now surfaces the liquid majors, which co-move with BTC, so in a market fall (the H-L063 abort scenario) every long draws down together -- adding concurrent names cannot diversify systematic risk, which is why the fix is to lower the total-exposure ceiling via risk_per_trade and maximum_position_fraction rather than to spread the same risk wider. Isolating those two sizing levers (distinct from H-L068's exit-discipline tightening, which failed the gate only on the defective universe) tests aggregate-exposure vs per-trade-tail as the drawdown mechanism, and the kill condition routes the loop to exits if this is wrong.

