
## 2026-08-12T21:35:26+00:00 · iteration 49

**Claim** Replacing the fixed absolute morning bar with a volatility-normalized one — enter when the 06:00 UTC move exceeds the 0.85 percentile of that asset's own trailing 20-day distribution of morning moves — fires at a roughly regime-invariant rate and produces at least 20 trades with a positive return in the sealed 2026 window, versus the incumbent's 3 trades and +0.05%, while training drawdown stays under 25% via peak-basis deleverage.

**Needs built** The existing volatility_quantile/volatility_window_days parameters wire to a volatility-regime gate applied on top of the absolute itsm_threshold, not to a normalized entry bar. What must be built: an itsm entry that fires when the current 06:00 move ranks above volatility_quantile within that symbol's own trailing volatility_window_days distribution of morning moves, with itsm_threshold demoted to a low absolute floor. Until then this genome is queued rather than run.

**Reasoning** The absolute-threshold curve is now fully mapped and its forward failure is diagnosed: threshold 3.0% trains best (+182% at 23.5%) yet died forward (3 trades, +0.05%) because 2026 simply did not produce 3% morning moves — the entry was refused 319,662 times while the trend filter refused only 10. The briefing states the durable fix outright: 'a bar measured in units of each asset's own volatility rather than in absolute percent.' Lower absolute thresholds trade enough forward (1.5% → 24 trades, +5.05% sealed) but only sit legally under the mandate at 24.6% — right at the edge — and are already in the ledger, so moving along that mapped curve adds little. A percentile bar answers the briefing's own test ('how often does it fire when daily ranges are small') by construction: it targets a fixed firing rate, so a small move in a quiet year still qualifies if it is large relative to that asset's recent range. This is distinct from the refuted signal-scaling (risk is unchanged) and from the -0.01 config, which layered a volatility-regime filter ON TOP of the absolute 3% bar on the wide universe — conflating two effects and gating to high-vol regimes rather than normalizing the entry bar itself.

## 2026-08-12T21:44:17+00:00 · iteration 51

**Claim** Replacing the fixed absolute morning bar with a volatility-relative one -- requiring the 00:00->06:00 return to exceed k times each asset's own trailing daily volatility (window = volatility_window_days) instead of a fixed itsm_threshold percent -- holds trade frequency roughly stable across quiet and violent years (order 25-40 trades/year in both a 2018-like and a 2026-like regime) while preserving the monotone right-tail excess-over-drift that H-INTRA-002 established, because the same k maps t

**Needs built** The itsm entry compares the morning return to an absolute itsm_threshold. Add a mode where the bar is k * trailing per-asset realized volatility (ATR or return-stdev over volatility_window_days), evaluated per symbol at itsm_hour. Simplest wiring: reinterpret itsm_threshold as the multiplier k when a new flag itsm_threshold_units='vol' is set (reuse volatility_window_days for the lookback; leave volatility_quantile unused here). No other module changes -- holding, exits, sizing, and the mandate stay as-is.

**Reasoning** H-INTRA-002 is monotone in the bar in both eras and pays through its right tail, so the mechanism is real; the failure is purely how the bar is denominated. The sealed forensics are decisive: at 3.0% the entry was refused 319,662 times in 2026 (trend filter only 10) and 2026 simply produced no 3% morning moves, yet the same config trades 34x in 2018 and 31x in 2022 -- a fixed percent is a moving target across volatility regimes. 1.5% is the true forward edge (+5.05%) but breaches the 25% mandate on initial-basis in training. A bar of k*trailing-vol per asset is the one denomination that fires at a comparable rate whether daily ranges are 1% or 6%. This is distinct from the refuted volatility_quantile experiments, which gated on realized-vol regime while leaving the absolute threshold in place (rows -0.01 and -0.90); those filter on top of the wrong bar rather than replacing it.

## 2026-08-12T22:27:09+00:00 · iteration 62

**Claim** Replacing the fixed absolute morning bar with a volatility-normalized one — buy at 06:00 UTC when the day's return exceeds `multiple × NATR_14` (the asset's own trailing 14-bar volatility, i.e. survey.py's `itsm_vol`: day_return >= multiple*natr_14/100*12), holding ~864 bars, itsm_threshold demoted to a small noise floor — fires at a far more regime-stable annual rate than the 3% bar: across the eight training years its trade count varies far less (leanest year >= ~20 trades vs the incumbent's c

**Needs built** Promote `itsm_vol` from trading-system/quantlab_intraday/survey.py into the production brain: add it to entry_rule CHOICES in orchestrator-manager/quantlab_manager/intraday_loop.py and to the `_qualifies` dispatch in trading-system/quantlab_intraday/momentum.py, reusing the existing `close/opened-1 >= multiple*natr_14/100*12` form. Expose `vol_multiple` (float, ~0.5-5.0) as a tunable genome parameter so the loop can calibrate firing frequency, and keep `itsm_threshold` as a small absolute noise floor ANDed with the vol condition. No new indicator is required — natr_14 is already served.

**Reasoning** The diagnosis spent a sealed window proving the incumbent's failure is not a bear-market stand-aside but that `a fixed 3% morning bar is a different rule in a quiet year than a violent one` — 2026 simply produced no 3% morning moves, so the entry was refused 319,662 times while the trend filter fired only 10. The diagnosis names the durable fix explicitly: a bar in units of each asset's own volatility. The code already contains it — `itsm_vol` in trading-system/quantlab_intraday/survey.py measures the morning move against natr_14, with the docstring `A fixed 1.5% is a different rule on BTC than on SOL` — but the loop's entry_rule CHOICES are only ('itsm','donchian','volexp'), so it has never reached a backtest. This is NOT the exhausted volexp path (11 ledger runs it4..it61, all mandate breaches at the same 2018 peak) nor the vol-quantile veto layered on the absolute bar (it2/it40/it44/it49/it54/it57/it59, all failed) — the repo's own needs-code.md flags both of those as gates on the wrong bar and lists the true vol-normalized entry as queued-not-built. It keeps the mechanism that actually pays (06:00 momentum, hold ~3 days, no stop truncating the right tail — all settled) and changes only the yardstick, so it should fire ~24x in quiet 2026 the way threshold 1.5% did (+5.05% sealed) without threshold 1.5%'s regime-brittle absolute bar.

## 2026-08-12T22:31:59+00:00 · iteration 63

**Claim** Releasing the absolute morning bar to its non-binding floor (itsm_threshold=0.002) and gating entries on a per-asset volatility quantile (volatility_quantile=0.6 over a 20-day window) will make the ITSM entry fire at least ~15 times per year in EVERY historical year and in 2026 — unlike the fixed 3% bar, which fired 3 times in 2026 because a 3% morning move is a different event in a quiet year than a violent one — while surviving the 25% mandate on drawdown_basis=peak.

**Needs built** If volatility_quantile gates on the percentile of realized volatility/ATR rather than on the percentile of each asset's own recent 06:00-to-06:00 morning returns, then this genome cannot express the intended mechanism and the kill condition will fire. The genuine durable fix is: define the entry bar as itsm_threshold expressed in units of the asset's trailing volatility (e.g. required_move = k × ATR%_over_window, or equivalently 'today's morning return exceeds the q-quantile of this symbol's trailing N-day morning returns'), so the bar auto-scales to each symbol's regime instead of being a fixed absolute percent. Queue this if the parameter path above is confounded by realized-vol semantics.

**Reasoning** The hardest settled result cost a sealed window: itsm_threshold=0.03 trains best (+182%/23.5%, survives all eight years) but returned +0.05% on 3 trades in 2026, while itsm_threshold=0.015 returned +5.05% on 24 trades — because a fixed absolute bar is regime-dependent and 2026 produced no 3% morning moves (entry refused 319,662 times, trend filter only ten). The briefing's own conclusion is that the durable fix is a bar in units of each asset's own volatility. Every prior volatility-quantile config confounded the test by keeping a BINDING absolute threshold on top of the quantile (0.7+0.03 wide → 252 trades, -0.4%; 0.85+0.005 → book stood still), so the quantile was never tested as the primary, threshold-replacing gate. This isolates that: floor the absolute bar, let the per-asset quantile define 'a large morning move for THIS asset.' drawdown_basis=peak because the same threshold-0.015 rule already survives at 24.6% on peak basis (score +5.42) where it breached on initial basis — the correct basis for a continuously-running book. Five-universe because the wide universe is settled to buy trade count at the cost of drawdown via shared-factor high-beta names.

## 2026-08-12T22:45:46+00:00 · iteration 66

**Claim** Replacing the fixed-percent morning bar with entry_rule='volexp' — a breakout scaled to each asset's own recent volatility (0.80 quantile of a 14-day vol window) — will hold trade frequency roughly stable across regimes (≥20 trades in both a violent year like 2018 and a quiet one like 2024) while preserving the right-tail payoff, so it will not collapse to a handful of trades the way the fixed 3% bar did (3 trades, +0.05%) in the quiet 2026 window.

**Needs built** Only if entry_rule='volexp' turns out to be a generic anytime volatility-expansion breakout rather than the specific mechanism the diagnosis calls for. The durable fix is a normalized ITSM entry: at itsm_hour, buy when the return-since-session-open exceeds k times the asset's realized volatility (ATR or stdev) over volatility_window_days — i.e. itsm_threshold denominated in vol units instead of absolute percent. If volexp does not already evaluate the morning move at a fixed hour normalized by own-asset volatility, that entry rule must be built.

**Reasoning** The sealed-window forensics identified the real defect precisely: a fixed 3% morning move is a different rule in a quiet year than a violent one, and 2026 simply did not produce 3% mornings (entry refused 319,662 times, trend filter only 10). The briefing names the durable fix — a bar in units of each asset's own volatility, not absolute percent. Every prior volatility experiment in the ledger kept entry_rule=itsm and merely stacked a vol-quantile GATE on top of the absolute threshold, which either subtracted return (score -0.01 at q0.7/w10) or froze the book (1% exposure at q0.6/q0.85). entry_rule='volexp' is the one untried mechanism that expresses the entry itself in volatility units rather than filtering an absolute one, and donchian's 0-trade result shows the alternative rules are genuinely distinct code paths worth measuring. This is the cheapest possible information on the exact thesis the diagnosis endorses, and it does not touch any settled finding (no stop, no trailing, no extra positions, no wider universe, no signal-scaling).

## 2026-08-12T22:53:40+00:00 · iteration 68

**Claim** Replacing the fixed-percent morning bar with entry_rule=volexp -- a morning move scaled to each asset's own recent realized volatility (Crabel's Contraction-Expansion breakout) -- will keep the trade count regime-stable, firing at least 20 trades in its leanest calendar year instead of collapsing to a handful in a quiet year, while preserving the right-tail hold that the settled evidence shows must not be truncated.

**Needs built** Only if entry_rule=volexp is NOT already a morning-return-normalized-by-own-recent-volatility breakout gated at itsm_hour. The mechanism that must exist is: fire the buy at itsm_hour when (close[itsm_hour] - day_open) / realized_vol(volatility_window_days) exceeds a tunable multiple k expressed in volatility units -- i.e. the entry bar denominated in each asset's own sigma rather than in absolute percent -- with everything downstream (hold ~864 bars, no stop truncation) unchanged.

**Reasoning** The lab's hardest finding, paid for with a sealed window, is that a fixed 3% morning bar is a different rule in a quiet year than a violent one -- 2026 refused the entry 319,662 times and produced 3 trades / +0.05% versus 24 trades / +5.05% at 1.5%. Gao-Han-Li-Zhou (Market Intraday Momentum, JFE 2018, SSRN 2440866) show the intraday-momentum edge is an increasing function of volatility, so the signal SHOULD be volatility-conditioned -- but Crabel (1990, Contraction-Expansion / opening-range breakout; see Umea working paper ues845 on OR/ATR normalization) shows the condition must be measured relative to each asset's own recent range, not in absolute percent. The lab already refuted a volatility_quantile gate LAYERED ON the fixed threshold (an intersection that froze the book at ~1% exposure). volexp is the untested rule that IS the normalized entry rather than a gate on top of an absolute one; donchian was tried and never fired, but volexp has never reached a backtest. Loose vol params (quantile 0.6, 14-day window) are chosen specifically to avoid the frozen-book regime that the 0.85-quantile runs hit.

## 2026-08-12T22:57:28+00:00 · iteration 69

**Claim** Replacing the fixed absolute 06:00 entry bar with a per-asset volatility-normalized bar (buy when the day's move exceeds k times the asset's own trailing daily range over volatility_window_days) makes trade frequency regime-invariant: it will fire a comparable number of trades in quiet and violent years while preserving the monotone right-tail edge, and unlike the fixed 3% bar it will still trade (>=15 trades) and stay net-positive in a low-volatility forward window.

**Needs built** itsm_threshold is an absolute fraction with no way to express 'threshold in units of the asset's own volatility'. Need code that, per asset per bar, computes a trailing daily-range (or ATR) baseline over volatility_window_days and gates the 06:00 entry on day_return >= k * baseline, with k as the tuned parameter. The current volatility_quantile filter is not a substitute -- it restricts to high-vol bars (froze the book at quantiles 0.6-0.85) rather than rescaling the bar.

**Reasoning** The ITSM edge itself is settled and real (excess-over-drift monotone in threshold in BOTH eras, paid through the right tail). What failed was the units: a fixed 3% bar refused entry 319,662 times in 2026 because that year produced no 3% morning moves, while the 1.5% bar traded 24x for +5.05%. A fixed percent is therefore a different rule in a quiet year than a violent one -- exactly the failure the sealed window exposed. Normalizing the bar by each asset's own trailing range (ATR-style) keeps the mechanism but removes its dependence on the ambient volatility regime, which is the only lever the ledger has not yet been able to pull.
