## Iteration 62 — proposal

**Module:** SIDEWAYS

**Claim:** A range-breakout long entry -- close crossing UP through bb_upper on above-average volume (volume > volume_sma_20) while ADX is still sub-25 (a range that is only now resolving), exiting when close crosses back down through bb_mid -- clears the SIDEWAYS evolved fold-fit gate, because in a range regime the profitable long is not the fade of the lower band (killed in H-L057) but the participation-confirmed escape from the top of the range, a mechanism this module has never been given.

**Killed by:** Refuted if the evolved fold fit score does not exceed SIDEWAYS's best-known fit and the forward window is therefore not opened; and, should a window open, refuted if SIDEWAYS's own attributed contribution (closed + mark-to-market per H-L059R) is <= 0.

Two ledger facts bound this. (1) H-L057 killed the lower-band mean-reversion dip-buy for SIDEWAYS explicitly ('this direction is recorded as dead'); repeating any bb_percent_b/rsi fade would be a re-run. (2) H-L058R and, decisively, H-L060R establish that SIDEWAYS takes ZERO forward trades in 2026 at BOTH market and asset scope -- every one of the 212 bars resolves to BEAR -- so no SIDEWAYS rule can move the 2026 forward number. The genuine leverage is in DETECTOR (why the risers/ranges are never routed to their branch), and I flag that as advice. But the target is SIDEWAYS, and its only remaining falsifiable surface is the fold-fit gate over the 2020-2025 folds, where range regimes actually exist. So I deepen nothing dead: instead of fading the range, I test the opposite mechanism -- a participation-confirmed breakout from the top of the range while ADX is still low (breakout not yet reflected in a trend reading), taking profit on reversion to bb_mid. If this cannot even clear the SIDEWAYS fold gate, the module has no long edge distinct from BEAR's dip-buy and the next iteration should abandon SIDEWAYS rule-tuning entirely and move to DETECTOR routing.

- `(close crosses above bb_upper AND volume > volume_sma_20 AND adx < 25)`
- `close crosses below bb_mid`