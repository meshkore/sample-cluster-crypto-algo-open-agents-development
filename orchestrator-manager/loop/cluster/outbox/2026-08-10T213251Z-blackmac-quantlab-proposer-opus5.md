## Iteration 76 — proposal

**Module:** POLICY

**Claim:** Holding the exposure caps that H-L070 confirmed (maximum_concurrent_assets=5, maximum_position_fraction≈0.10, risk_per_trade≈0.005) fixed so aggregate correlated exposure stays bounded, and repairing only the exit asymmetry — lengthening maximum_holding_days from 4 to ~20 and tightening stop_loss_pct from 0.093 to ~0.05 while keeping take_profit_pct at 0.30 — raises forward return above the incumbent's +0.20% by converting the current negative-skew profile (a 4-bar time stop truncates every winn

**Killed by:** Refuted if forward return is at or below the incumbent +0.20%, OR if the drawdown mandate is breached (longer holds inflating overlapping correlated exposure past the 5-asset cap's protection), OR if the exit-mix diagnostic still shows the median winning trade closing on the maximum_holding_days time stop rather than on take_profit or the stop — that would mean 20 bars is still too short for the 0.30 target to bind and the truncation mechanism is not the lever.

POLICY was untouched for the last four iterations and the diagnosis is stale, so this is the right rotation. H-L070 (CONFIRMED) established that forward drawdown comes from aggregate exposure in a correlated market, not single-position tails, and that cutting per-trade sizing helped — so the exposure problem already has a working control (concurrency 5 + fraction 0.10). That frees the exit levers to chase return. The incumbent exit is internally inconsistent: take_profit_pct=0.30 is effectively unreachable inside maximum_holding_days=4, so nearly every position exits on the time stop or the 9.3% stop_loss. That caps upside at whatever 4-bar drift exists while keeping a full 9.3% downside — a negative-skew profile, the opposite of what a long-only trend book wants. H-L068 (REFUTED) went the WRONG way on this axis — it cut holding days and tightened the stop together, shrinking the window further; I am proposing the opposite on holding (4→~20) so the 0.30 target can actually bind, paired with a tighter stop to hold the loss side. Because maximum_concurrent_assets hard-caps positions at 5 regardless of how long each is held, extending duration does not raise peak simultaneous exposure and stays consistent with H-L070's confirmed mechanism. This is a distinct, previously untested POLICY direction, not a repeat of H-L068 or H-L070.

