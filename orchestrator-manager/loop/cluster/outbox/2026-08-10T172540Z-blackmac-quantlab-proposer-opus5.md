## Iteration 69 — proposal

**Module:** POLICY

**Claim:** Holding the detector and all module rule trees at incumbent, recalibrating ONLY the exposure/concentration levers to the new 100-asset book width -- cutting risk_per_trade by roughly a third and maximum_position_fraction so no single idea holds more than ~5-8% of the book, while RAISING maximum_concurrent_assets toward ~15-20 to spread the same-or-lower total risk across the wider dollar-volume universe, and leaving stop_loss_pct, take_profit_pct and maximum_holding_days at incumbent -- will hol

**Killed by:** Refuted if the run still aborts on the drawdown mandate (equity touches the peak floor) OR max_drawdown is not lower than the incumbent's OR forward return falls below +1.12% / the fit fails to clear the gate. Any of these means the mandate-breaching drawdown is driven by rule selection (which assets, when) rather than by POLICY sizing, and the search should leave POLICY exposure-tuning and route to DETECTOR (regime_scope) / BEAR.

POLICY takes no trades and has no rule tree, so the work is in the claim. This is deliberately distinct from the two dead POLICY directions: H-L063 was a vague 'move the params' with no mechanism (-20.02%), and H-L068 moved only the EXIT side (stop_loss 0.06, holding_days 10) while explicitly freezing every sizing and exposure cap -- and failed the gate, which is direct evidence the drawdown is not exit-timing driven. The untested complement is aggregate exposure and per-name concentration, which is precisely what the diagnosis flags: H-L068U confirmed the incumbent sizing was fitted on a 55-symbol alphabetical book, and we now deploy across 100 dollar-volume-selected assets from 386. The mandate abort in H-L063R (equity 79,977 <= floor 82,303, drawdown 31.98%) was a correlated portfolio-wide leg-down -- controlled by gross exposure and concentration, not by when individual positions exit. Cutting per-trade risk and per-name fraction while widening concentration across the larger, less-alphabetically-degenerate universe tests whether the mandate breach is a sizing artifact of the old book width. If it is, drawdown drops below the floor and return holds; if it is not, the kill condition fires and points the loop at the detector/BEAR selection instead.

