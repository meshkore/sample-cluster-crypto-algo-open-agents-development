# Training-path audit — 2026-09-05

Operator asked for a full audit of the model path: is it correct, where are the
faults, can the capabilities be expanded. Method: **measure, don't read** — every
claim below was established by a probe against the running code, not by trusting a
docstring. Each verdict names its evidence.

## Verdicts

| # | Seam | Verdict | Evidence |
|---|------|---------|----------|
| 1 | Feature causality (44 cols) | **CLEAN** | Truncation probe: features from `bars[:520]` are bit-for-bit identical to the first 520 rows computed from 700 bars — values AND NaN warm-up pattern. `tests/test_feature_causality.py`, 3 tests. |
| 2 | Standardiser | **CLEAN** | Fitted on a prefix, transform of the past identical with/without the future existing. Same file. |
| 3 | Model causality | **CLEAN** | Gradient probe: output at t has zero gradient to inputs > t. `tests/test_receptive_field.py`. |
| 4 | Model **reach** | **FAULT (A103)** | Same probe: the shipping champion sees 15 of its 96 bars; 81 are prepared and multiplied by nothing. Fix in training now (`_w192d6`, 6 blocks → reach 127). |
| 5 | Execution timing | **CLEAN (structural)** | `backtester/quantlab_backtester/session.py`: orders against tick N sit in a queue and fill at the open of tick N+1 — enforced by the session, not by convention. |
| 6 | fast_portfolio close-fill | **ACCEPTED, bounded** | Selection proxy only; documented; every published number re-runs through the real session. |
| 7 | Train/val seam | **SUSPECT (P52)** | Champion trained `embargo=0`; zigzag labels near the split know post-split prices through pivot confirmation. Window overlap IS prevented (gap of `window`), label leakage at the seam is not. Bounded and local, but the val numbers the loop selects on may be slightly flattered. Queued to quantify. |
| 8 | Net's own economics | **FAULT (A108)** | Champion model card: val_gross +158% → val_net −31%, beats B&H on 2/14 symbols. The strategy's profit comes from the layers around the net. Any "expand the model" work must move THIS number, or it decorates a blind engine. |
| 9 | Built-but-unwired capability | **WASTE (A104)** | `vectormem.py` finished + tested, referenced by nothing. Queued to wire (P53). |
| 10 | Ensemble capability | **OFF (A106)** | `train(ensemble=N)` implemented; champion trained with N=1. Queued (P51) after A103 picks the architecture. |

## The ordered programme (each result gates the next)

**GPU lane** (serial, one job owns the card):
1. `_w192d6` training now — 50 epochs, everything else verbatim champion.
2. **P50** A103 readout: first number to read is `val_net_return` vs −0.3128. Then
   paired held-out backtest. Sealed 2026 only if it wins there.
3. **P51** ensemble=5 of whichever architecture P50 selects.
4. **P52** embargo retrain (brief GPU) to price the seam.

**CPU lane** (parallel, never touches the card):
- thresholds-v3-heldout study to 2000 trials (6 workers, watchdog-owned).
- **P53** vectormem module wiring + A/B.
- A92 selection experiment / A101 deflated reporting on completed trials (free).

**Standing rule** (unchanged): selection on FIT 2018-2023, confirmation on held-out
2024-2025, sealed 2026 read once, deliberately, whatever it says.
