
## 2026-08-12T21:35:26+00:00 · iteration 49

**Claim** Replacing the fixed absolute morning bar with a volatility-normalized one — enter when the 06:00 UTC move exceeds the 0.85 percentile of that asset's own trailing 20-day distribution of morning moves — fires at a roughly regime-invariant rate and produces at least 20 trades with a positive return in the sealed 2026 window, versus the incumbent's 3 trades and +0.05%, while training drawdown stays under 25% via peak-basis deleverage.

**Needs built** The existing volatility_quantile/volatility_window_days parameters wire to a volatility-regime gate applied on top of the absolute itsm_threshold, not to a normalized entry bar. What must be built: an itsm entry that fires when the current 06:00 move ranks above volatility_quantile within that symbol's own trailing volatility_window_days distribution of morning moves, with itsm_threshold demoted to a low absolute floor. Until then this genome is queued rather than run.

**Reasoning** The absolute-threshold curve is now fully mapped and its forward failure is diagnosed: threshold 3.0% trains best (+182% at 23.5%) yet died forward (3 trades, +0.05%) because 2026 simply did not produce 3% morning moves — the entry was refused 319,662 times while the trend filter refused only 10. The briefing states the durable fix outright: 'a bar measured in units of each asset's own volatility rather than in absolute percent.' Lower absolute thresholds trade enough forward (1.5% → 24 trades, +5.05% sealed) but only sit legally under the mandate at 24.6% — right at the edge — and are already in the ledger, so moving along that mapped curve adds little. A percentile bar answers the briefing's own test ('how often does it fire when daily ranges are small') by construction: it targets a fixed firing rate, so a small move in a quiet year still qualifies if it is large relative to that asset's recent range. This is distinct from the refuted signal-scaling (risk is unchanged) and from the -0.01 config, which layered a volatility-regime filter ON TOP of the absolute 3% bar on the wide universe — conflating two effects and gating to high-vol regimes rather than normalizing the entry bar itself.

## 2026-08-12T21:44:17+00:00 · iteration 51

**Claim** Replacing the fixed absolute morning bar with a volatility-relative one -- requiring the 00:00->06:00 return to exceed k times each asset's own trailing daily volatility (window = volatility_window_days) instead of a fixed itsm_threshold percent -- holds trade frequency roughly stable across quiet and violent years (order 25-40 trades/year in both a 2018-like and a 2026-like regime) while preserving the monotone right-tail excess-over-drift that H-INTRA-002 established, because the same k maps t

**Needs built** The itsm entry compares the morning return to an absolute itsm_threshold. Add a mode where the bar is k * trailing per-asset realized volatility (ATR or return-stdev over volatility_window_days), evaluated per symbol at itsm_hour. Simplest wiring: reinterpret itsm_threshold as the multiplier k when a new flag itsm_threshold_units='vol' is set (reuse volatility_window_days for the lookback; leave volatility_quantile unused here). No other module changes -- holding, exits, sizing, and the mandate stay as-is.

**Reasoning** H-INTRA-002 is monotone in the bar in both eras and pays through its right tail, so the mechanism is real; the failure is purely how the bar is denominated. The sealed forensics are decisive: at 3.0% the entry was refused 319,662 times in 2026 (trend filter only 10) and 2026 simply produced no 3% morning moves, yet the same config trades 34x in 2018 and 31x in 2022 -- a fixed percent is a moving target across volatility regimes. 1.5% is the true forward edge (+5.05%) but breaches the 25% mandate on initial-basis in training. A bar of k*trailing-vol per asset is the one denomination that fires at a comparable rate whether daily ranges are 1% or 6%. This is distinct from the refuted volatility_quantile experiments, which gated on realized-vol regime while leaving the absolute threshold in place (rows -0.01 and -0.90); those filter on top of the wrong bar rather than replacing it.

## 2026-08-12T22:27:09+00:00 · iteration 62

**Claim** Replacing the fixed absolute morning bar with a volatility-normalized one — buy at 06:00 UTC when the day's return exceeds `multiple × NATR_14` (the asset's own trailing 14-bar volatility, i.e. survey.py's `itsm_vol`: day_return >= multiple*natr_14/100*12), holding ~864 bars, itsm_threshold demoted to a small noise floor — fires at a far more regime-stable annual rate than the 3% bar: across the eight training years its trade count varies far less (leanest year >= ~20 trades vs the incumbent's c

**Needs built** Promote `itsm_vol` from trading-system/quantlab_intraday/survey.py into the production brain: add it to entry_rule CHOICES in orchestrator-manager/quantlab_manager/intraday_loop.py and to the `_qualifies` dispatch in trading-system/quantlab_intraday/momentum.py, reusing the existing `close/opened-1 >= multiple*natr_14/100*12` form. Expose `vol_multiple` (float, ~0.5-5.0) as a tunable genome parameter so the loop can calibrate firing frequency, and keep `itsm_threshold` as a small absolute noise floor ANDed with the vol condition. No new indicator is required — natr_14 is already served.

**Reasoning** The diagnosis spent a sealed window proving the incumbent's failure is not a bear-market stand-aside but that `a fixed 3% morning bar is a different rule in a quiet year than a violent one` — 2026 simply produced no 3% morning moves, so the entry was refused 319,662 times while the trend filter fired only 10. The diagnosis names the durable fix explicitly: a bar in units of each asset's own volatility. The code already contains it — `itsm_vol` in trading-system/quantlab_intraday/survey.py measures the morning move against natr_14, with the docstring `A fixed 1.5% is a different rule on BTC than on SOL` — but the loop's entry_rule CHOICES are only ('itsm','donchian','volexp'), so it has never reached a backtest. This is NOT the exhausted volexp path (11 ledger runs it4..it61, all mandate breaches at the same 2018 peak) nor the vol-quantile veto layered on the absolute bar (it2/it40/it44/it49/it54/it57/it59, all failed) — the repo's own needs-code.md flags both of those as gates on the wrong bar and lists the true vol-normalized entry as queued-not-built. It keeps the mechanism that actually pays (06:00 momentum, hold ~3 days, no stop truncating the right tail — all settled) and changes only the yardstick, so it should fire ~24x in quiet 2026 the way threshold 1.5% did (+5.05% sealed) without threshold 1.5%'s regime-brittle absolute bar.

## 2026-08-12T22:31:59+00:00 · iteration 63

**Claim** Releasing the absolute morning bar to its non-binding floor (itsm_threshold=0.002) and gating entries on a per-asset volatility quantile (volatility_quantile=0.6 over a 20-day window) will make the ITSM entry fire at least ~15 times per year in EVERY historical year and in 2026 — unlike the fixed 3% bar, which fired 3 times in 2026 because a 3% morning move is a different event in a quiet year than a violent one — while surviving the 25% mandate on drawdown_basis=peak.

**Needs built** If volatility_quantile gates on the percentile of realized volatility/ATR rather than on the percentile of each asset's own recent 06:00-to-06:00 morning returns, then this genome cannot express the intended mechanism and the kill condition will fire. The genuine durable fix is: define the entry bar as itsm_threshold expressed in units of the asset's trailing volatility (e.g. required_move = k × ATR%_over_window, or equivalently 'today's morning return exceeds the q-quantile of this symbol's trailing N-day morning returns'), so the bar auto-scales to each symbol's regime instead of being a fixed absolute percent. Queue this if the parameter path above is confounded by realized-vol semantics.

**Reasoning** The hardest settled result cost a sealed window: itsm_threshold=0.03 trains best (+182%/23.5%, survives all eight years) but returned +0.05% on 3 trades in 2026, while itsm_threshold=0.015 returned +5.05% on 24 trades — because a fixed absolute bar is regime-dependent and 2026 produced no 3% morning moves (entry refused 319,662 times, trend filter only ten). The briefing's own conclusion is that the durable fix is a bar in units of each asset's own volatility. Every prior volatility-quantile config confounded the test by keeping a BINDING absolute threshold on top of the quantile (0.7+0.03 wide → 252 trades, -0.4%; 0.85+0.005 → book stood still), so the quantile was never tested as the primary, threshold-replacing gate. This isolates that: floor the absolute bar, let the per-asset quantile define 'a large morning move for THIS asset.' drawdown_basis=peak because the same threshold-0.015 rule already survives at 24.6% on peak basis (score +5.42) where it breached on initial basis — the correct basis for a continuously-running book. Five-universe because the wide universe is settled to buy trade count at the cost of drawdown via shared-factor high-beta names.

## 2026-08-12T22:45:46+00:00 · iteration 66

**Claim** Replacing the fixed-percent morning bar with entry_rule='volexp' — a breakout scaled to each asset's own recent volatility (0.80 quantile of a 14-day vol window) — will hold trade frequency roughly stable across regimes (≥20 trades in both a violent year like 2018 and a quiet one like 2024) while preserving the right-tail payoff, so it will not collapse to a handful of trades the way the fixed 3% bar did (3 trades, +0.05%) in the quiet 2026 window.

**Needs built** Only if entry_rule='volexp' turns out to be a generic anytime volatility-expansion breakout rather than the specific mechanism the diagnosis calls for. The durable fix is a normalized ITSM entry: at itsm_hour, buy when the return-since-session-open exceeds k times the asset's realized volatility (ATR or stdev) over volatility_window_days — i.e. itsm_threshold denominated in vol units instead of absolute percent. If volexp does not already evaluate the morning move at a fixed hour normalized by own-asset volatility, that entry rule must be built.

**Reasoning** The sealed-window forensics identified the real defect precisely: a fixed 3% morning move is a different rule in a quiet year than a violent one, and 2026 simply did not produce 3% mornings (entry refused 319,662 times, trend filter only 10). The briefing names the durable fix — a bar in units of each asset's own volatility, not absolute percent. Every prior volatility experiment in the ledger kept entry_rule=itsm and merely stacked a vol-quantile GATE on top of the absolute threshold, which either subtracted return (score -0.01 at q0.7/w10) or froze the book (1% exposure at q0.6/q0.85). entry_rule='volexp' is the one untried mechanism that expresses the entry itself in volatility units rather than filtering an absolute one, and donchian's 0-trade result shows the alternative rules are genuinely distinct code paths worth measuring. This is the cheapest possible information on the exact thesis the diagnosis endorses, and it does not touch any settled finding (no stop, no trailing, no extra positions, no wider universe, no signal-scaling).

## 2026-08-12T22:53:40+00:00 · iteration 68

**Claim** Replacing the fixed-percent morning bar with entry_rule=volexp -- a morning move scaled to each asset's own recent realized volatility (Crabel's Contraction-Expansion breakout) -- will keep the trade count regime-stable, firing at least 20 trades in its leanest calendar year instead of collapsing to a handful in a quiet year, while preserving the right-tail hold that the settled evidence shows must not be truncated.

**Needs built** Only if entry_rule=volexp is NOT already a morning-return-normalized-by-own-recent-volatility breakout gated at itsm_hour. The mechanism that must exist is: fire the buy at itsm_hour when (close[itsm_hour] - day_open) / realized_vol(volatility_window_days) exceeds a tunable multiple k expressed in volatility units -- i.e. the entry bar denominated in each asset's own sigma rather than in absolute percent -- with everything downstream (hold ~864 bars, no stop truncation) unchanged.

**Reasoning** The lab's hardest finding, paid for with a sealed window, is that a fixed 3% morning bar is a different rule in a quiet year than a violent one -- 2026 refused the entry 319,662 times and produced 3 trades / +0.05% versus 24 trades / +5.05% at 1.5%. Gao-Han-Li-Zhou (Market Intraday Momentum, JFE 2018, SSRN 2440866) show the intraday-momentum edge is an increasing function of volatility, so the signal SHOULD be volatility-conditioned -- but Crabel (1990, Contraction-Expansion / opening-range breakout; see Umea working paper ues845 on OR/ATR normalization) shows the condition must be measured relative to each asset's own recent range, not in absolute percent. The lab already refuted a volatility_quantile gate LAYERED ON the fixed threshold (an intersection that froze the book at ~1% exposure). volexp is the untested rule that IS the normalized entry rather than a gate on top of an absolute one; donchian was tried and never fired, but volexp has never reached a backtest. Loose vol params (quantile 0.6, 14-day window) are chosen specifically to avoid the frozen-book regime that the 0.85-quantile runs hit.

## 2026-08-12T22:57:28+00:00 · iteration 69

**Claim** Replacing the fixed absolute 06:00 entry bar with a per-asset volatility-normalized bar (buy when the day's move exceeds k times the asset's own trailing daily range over volatility_window_days) makes trade frequency regime-invariant: it will fire a comparable number of trades in quiet and violent years while preserving the monotone right-tail edge, and unlike the fixed 3% bar it will still trade (>=15 trades) and stay net-positive in a low-volatility forward window.

**Needs built** itsm_threshold is an absolute fraction with no way to express 'threshold in units of the asset's own volatility'. Need code that, per asset per bar, computes a trailing daily-range (or ATR) baseline over volatility_window_days and gates the 06:00 entry on day_return >= k * baseline, with k as the tuned parameter. The current volatility_quantile filter is not a substitute -- it restricts to high-vol bars (froze the book at quantiles 0.6-0.85) rather than rescaling the bar.

**Reasoning** The ITSM edge itself is settled and real (excess-over-drift monotone in threshold in BOTH eras, paid through the right tail). What failed was the units: a fixed 3% bar refused entry 319,662 times in 2026 because that year produced no 3% morning moves, while the 1.5% bar traded 24x for +5.05%. A fixed percent is therefore a different rule in a quiet year than a violent one -- exactly the failure the sealed window exposed. Normalizing the bar by each asset's own trailing range (ATR-style) keeps the mechanism but removes its dependence on the ambient volatility regime, which is the only lever the ledger has not yet been able to pull.

## 2026-08-12T23:02:45+00:00 · iteration 70

**Claim** Lowering the absolute morning bar from 3.0% to 1.5% under the incumbent's initial drawdown basis on the five-asset universe (volatility veto off) will keep at least ~30 trades in every calendar year, with no quiet-year collapse to single digits like the 3% bar's {2023:3, 2024:1}, while holding max drawdown under 25% — showing the 3% bar's forward failure (3 trades, +0.05% in 2026) was trade-count starvation, not edge decay, and motivating a volatility-relative entry bar.

**Needs built** Promote the already-surveyed volatility-relative ITSM entry into the runtime IntradayMomentumBrain as a new entry_rule option 'itsm_vol'. The formula exists at survey.py:231-251 (day_return >= multiple * natr_14/100 * 12) but is not wired into the executable brain (entry_rule is validated to itsm|donchian|volexp at momentum.py:177). Add it with a tunable 'itsm_atr_multiple' that replaces the absolute itsm_threshold, so the morning bar is measured in units of each asset's own NATR and fires at a stable frequency in both quiet and violent years -- the direct answer to 'how often does the bar fire when daily ranges are small'.

**Reasoning** Code confirms the settled diagnosis: momentum.py:406 tests close/day_open-1 against an absolute itsm_threshold, so a fixed 3% bar is a different rule in a quiet year (2026 produced almost no 3% morning moves -> entry refused 319,662 times, 3 trades) than in a violent one (34 trades in 2018). The sealed window already showed the lower bar wins forward: 1.5% -> 24 trades / +5.05% vs 3.0% -> 3 trades / +0.05%. The existing volatility parameters CANNOT test the real thesis: context.py:62-79 makes volatility_quantile a per-bar NATR blow-off veto stacked on top of the absolute bar (both must pass), which is why every ledger row using it froze (1.01% exposure) or degraded (-0.4%) -- it only removes trades, it never keeps slots filled in a quiet regime. This genome isolates one falsifiable step of the chain -- that 1.5% keeps the book trading across quiet years -- on a config absent from the ledger (1.5% has only ever been run with drawdown_basis=peak; pairing it with the initial basis that gave the incumbent its top score is untested). If confirmed, the mechanism is trade-count starvation and the durable answer is the volatility-relative bar in needs_code.

## 2026-08-12T23:23:25+00:00 · iteration 75

**Claim** An ITSM morning-momentum entry whose 06:00 UTC bar is set at k×(each asset's own trailing daily volatility) rather than a fixed 3% will fire a comparable number of trades per asset in quiet years (2023, 2024, 2026) and violent years (2018, 2021) — curing the regime-dependence that gave threshold-3% only 3 trades and +0.05% in 2026 — while preserving the monotone, positive excess-over-drift that the momentum mechanism pays through its right tail.

**Needs built** An ITSM entry variant whose 06:00 threshold is k × the asset's own trailing daily volatility (ATR or realized sigma over volatility_window_days), replacing the fixed-percent itsm_threshold. It must be per-asset (each symbol normalized by its own vol), applied as the entry bar (not as an added quantile gate like the current volatility_quantile), and calibrated so a single k targets a roughly constant per-asset trade rate across eras. Expose k as a tunable (e.g. reuse signal_scale_cap's slot or add itsm_atr_mult) since itsm_threshold is absolute percent and cannot carry a volatility multiple.

**Reasoning** The forensics on the hardest lesson are explicit: threshold 3.0% trains best of everything (+182% at 23.5%, score +7.76) yet produced 3 trades and +0.05% in the sealed 2026 window because the entry condition was refused 319,662 times against only 10 trend refusals — 2026 simply did not produce 3% morning moves, while the same rule traded 34× in 2018 and 31× in 2022. The briefing itself names the durable fix: 'a bar measured in units of each asset's own volatility rather than in absolute percent.' Crucially, this is NOT the same as the volatility_quantile experiments already in the ledger (scores -0.01 to -0.90, 'the book stood still'): those bolted a restrictive volatility GATE on top of the fixed itsm_threshold and suppressed trading. This proposal instead reparametrizes the threshold itself — the same morning-momentum mechanism, same 3-day hold, same no-stop (stop_atr=60, settled that truncation costs money), same 3-position cap and five-asset universe — changing only how the entry bar is measured. No current parameter expresses the bar in volatility units, so this is a genuinely new mechanism rather than a repeat of any ledger row.

## 2026-08-12T23:35:24+00:00 · iteration 78

**Claim** Replacing the fixed absolute morning bar with one measured in units of each asset's own trailing volatility — enter when the 06:00-UTC same-day return exceeds k·σ (k≈1.0, σ = std of daily returns over ~14 days) instead of a flat 3% — holds the entry's firing rate roughly constant across quiet and violent years, producing >20 trades in 2026 rather than 3, while preserving the positive per-trade excess over drift, so forward return moves off threshold-3%'s +0.05% toward threshold-1.5%'s ~+5% witho

**Needs built** Add itsm_threshold expressed in volatility units: at 06:00 UTC compute each asset's trailing volatility (std of daily log-returns over volatility_window_days, or ATR%), and require the same-day return to exceed itsm_threshold_sigma·σ rather than a fixed percent. Reuse the existing itsm_threshold field as the multiplier k (rescaled range ~0.3–3.0) and volatility_window_days as the lookback; leave the absolute-percent path as a fallback flag so the control run below can be paired against the normalized run on identical windows.

**Reasoning** The sealed 3% run is the decisive evidence: same config that trades 34x in 2018 and 31x in 2022 refused the entry 319,662 times in 2026 and took 3 trades for +0.05%, because a fixed 3% morning move is a rare event in a quiet year and a common one in a violent year — the bar's meaning drifts with the regime. The excess-over-drift edge is monotone in threshold, so I do not want to lower the bar and surrender per-trade edge; I want to keep a demanding bar that stays demanding-relative-to-regime, which is exactly a volatility-unit threshold (constant-frequency signal design). This also respects the lab's finding that training fit anti-predicts 2026 — raising the absolute bar maximized the training score and produced nothing forward. Note this is NOT the volatility_quantile parameter, which is a separate gate that killed the book (rows at 0.98–1.01% average exposure); that filters WHETHER volatility is high, it does not measure the morning MOVE in volatility units.

## 2026-08-12T23:39:07+00:00 · iteration 79

**Claim** Replacing the absolute 3% morning bar with a volatility-normalized entry (entry_rule=volexp, firing on the ~70th-percentile morning move over a 14-day realized-vol window) will hold trade frequency roughly stable across quiet and violent training years — leanest-year trade count within ~2x of its richest year, and comparable to the 1.5% absolute bar's ~24 trades in a 7-month slice — because the bar auto-scales to each regime's realized volatility instead of demanding a fixed 3% move that only vi

**Needs built** Only if volexp is NOT anchored to the itsm_hour morning bar: the intended mechanism is the *cumulative move from session open to 06:00 UTC, divided by that asset's trailing ATR/realized-vol*, thresholded at a quantile — not a generic all-day volatility-expansion breakout. If volexp is the latter (fires on any bar's range spike), it is a different rule than the one the diagnosis calls for, and a morning-anchored, vol-normalized itsm variant must be built. Confirm volexp's anchor before trusting the trade-count comparison.

**Reasoning** The sealed window already spent the verdict on the 3% direction: it trains best (+182% at 23.5%) yet fired 3 trades / +0.05% in 2026 because a fixed absolute bar is a *different rule* in a quiet year — the entry was refused 319,662 times, not the trend filter. The briefing names the durable fix explicitly: a bar in units of each asset's own volatility. I cannot instead just adopt the 1.5% bar — its +5.05% is a sealed-window observation and tuning toward it is using forward data as feedback, forbidden. So the only non-overfit move is a mechanism that generalizes. volexp is the one listed entry rule that normalizes the morning move by recent realized volatility, and it is untested (only itsm and a 0-trade donchian appear in the ledger). This changes exactly ONE thing versus the incumbent — the entry from absolute-percent to vol-relative — holding universe=five, no stops (trail/stop both settled as winner-truncating), 3 positions, hold 864, trend_ma 30, so any change in the regime-dependence of trade count is attributable to the normalization alone.

## 2026-08-12T23:44:00+00:00 · iteration 80

**Claim** Replacing the absolute morning-return bar with a self-scaling one -- enter at 06:00 UTC when the morning return exceeds k times the anchor's own trailing realized volatility over volatility_window_days -- will fire a comparable number of trades (~20+/yr) in BOTH quiet and violent years, curing the collapse where a fixed 3% bar produced only 3 trades in 2026, while preserving the right-tail per-trade excess-over-drift that the +2.49%-at-3.0% monotonicity is made of.

**Needs built** A volatility-relative entry bar. Compute per-anchor trailing realized volatility sigma (std of daily returns, or ATR/price) over volatility_window_days, then enter when the 06:00-UTC morning return >= k * sigma, where k is a new tunable multiple (suggest search 0.5-3.0). itsm_threshold stays only as a near-inert absolute floor (0.002); the sigma-bar does the selection. No current parameter expresses a threshold in volatility units -- itsm_threshold is absolute percent and volatility_quantile gates on vol level, not on move-size-relative-to-vol.

**Reasoning** The hardest sealed lesson is that a fixed 3% morning bar is a different rule in a quiet year than a violent one -- 2026 refused the entry 319,662 times not because the market was bearish (it trades 34x in 2018, 31x in 2022) but because 2026 never produced 3% morning moves; the briefing names the durable fix as a bar in units of each asset's own volatility. Published crypto intraday-momentum work supports the exact construction: Shen, Urquhart & Wang (2022, Financial Review, 'Bitcoin intraday time series momentum', centaur.reading.ac.uk/100181) find intraday TSM predictability is concentrated in the highest-volatility formation sessions, and the volatility-scaling TSM literature (Baltas & Kosowski; 'Time series momentum and volatility scaling', researchgate 303846490) shows scaling the signal to trailing sigma is what makes the edge stationary across regimes. This is NOT a repeat of the ledger's volatility_quantile=0.7/0.85 runs: those gated on volatility LEVEL (require high vol AND a move -> book stood still at ~1% exposure), whereas normalizing the bar fires roughly as often in a calm year as a stormy one -- the opposite failure mode. Keeping stop_atr=60/trail_atr=0 preserves the right tail the settled results say must never be truncated.

## 2026-08-12T23:47:11+00:00 · iteration 81

**Claim** Replacing the fixed absolute morning bar with a volatility-expansion entry (entry_rule=volexp, the 06:00 move judged against each asset's own recent range via volatility_window_days=20 / volatility_quantile=0.65) will fire 15-35 trades in the sealed 2026 window -- comparable to threshold-1.5% itsm's 24 -- rather than collapsing to 3 like the fixed 3.0% bar, while keeping training drawdown under 25% and return positive, because a bar denominated in volatility units scales down in a quiet year and

**Needs built** None assumed: volexp is an offered entry_rule and volatility_window_days/volatility_quantile are tunable, which implies the volatility-normalised entry is already wired. If the run reveals volexp keys off something other than the 06:00 morning move in volatility units, then what must be built is an itsm variant whose entry threshold is k*(ATR/close) rather than an absolute percent -- but that should be confirmed by this run before queueing it.

**Reasoning** The forensics already spent a sealed window proving the durable failure of the incumbent: the fixed 3.0% bar fired only 3 times in 2026 because 2026 produced no 3% morning moves -- 'a fixed 3% morning bar is a different rule in a quiet year than in a violent one' -- and the briefing names the fix explicitly: a bar measured in units of each asset's own volatility rather than absolute percent. entry_rule=volexp is the one entry mechanism never tried in the ledger (donchian gave 0 trades; every other row is itsm) and is the direct embodiment of that fix. Critically, the three prior volatility_quantile rows all kept entry_rule=itsm, using the quantile as an added GATE on top of the absolute 3% bar -- which either froze the book (0.98-1.01% exposure) or stripped out the winners (0.7 quantile turned +182% into -0.4%). None of them replaced the absolute threshold; this proposal does. All structural winners are held fixed (06:00 hour, ~3-day hold at 864 bars, no stop at stop_atr=60/trail 0 since truncating the right tail is settled to cost money, 3 positions, five-name universe, initial drawdown basis) so the entry mechanism is the only variable under test.

## 2026-08-12T23:55:23+00:00 · iteration 83

**Claim** An entry_rule=volexp bar that fires on each asset's top-quartile morning volatility (volatility_quantile=0.75 over a 14-day window), keeping the proven skeleton — no stop, ~3-day hold, three positions, five-name universe — will hold trade count roughly constant (~1 in 4 mornings per asset) across both quiet and violent training years, including 2023–2024 where the fixed 3% bar nearly stopped trading, while preserving a positive post-cost per-trade excess, and so will not repeat the trade-count c

**Needs built** Only if entry_rule=volexp is NOT a per-asset volatility-relative morning-move bar (e.g. it is an unrelated breakout channel). In that case the true durable fix is to express the itsm entry threshold as k×ATR (or k×σ) per asset — a volatility-normalized itsm_threshold — which is not a current tunable and would have to be built.

**Reasoning** The forensics on the incumbent are decisive: threshold 3.0% trains best (+182% at 23.5%) yet produced 3 trades and +0.05% in 2026 because the entry was refused 319,662 times — not a trend filter standing aside (that config trades 34x in 2018, 31x in 2022) but the fact that a fixed 3% absolute morning bar is a *different rule* in a quiet year, and 2026 produced no 3% mornings. The briefing's own durable fix is 'a bar measured in units of each asset's own volatility rather than in absolute percent,' and it demands an answer to 'how often does that fire when daily ranges are small.' A quantile of the asset's OWN recent distribution answers this mechanically: ~25% of mornings clear the 75th percentile by construction, whatever the absolute vol level, so trade frequency is regime-invariant — exactly the property the fixed bar lacks. I keep everything settled-good (truncating winners costs money → stop_atr=60/trail=0; more positions worse → 3; wider universe costs drawdown → five) and change only the entry. volexp is untried in the ledger; the only volatility-gated configs so far bolted the quantile onto itsm as a second gate, which is not this test.

## 2026-08-13T00:05:16+00:00 · iteration 85

**Claim** Replacing the absolute-percent morning bar with the already-implemented ATR-normalized volexp entry (a bar >=2 ATR wide closing on its high on a 3x volume spike), while keeping the incumbent's frame (864-bar hold, stop_atr=60 i.e. effectively no stop, trail_atr=0, trend_ma_days=30, 3 positions, five-name universe), will fire a trade count that stays within a comparable order of magnitude across quiet and violent training years -- because the trigger scales with each asset's own ATR -- instead of

**Needs built** To test the durable fix on the PROVEN morning-momentum edge itself rather than on a different entry rule: add an itsm variant whose threshold is expressed in the asset's own recent volatility -- trigger when (close/open-1) >= K * trailing NATR (or stdev of returns over volatility_window_days) instead of the fixed absolute itsm_threshold at momentum.py:406 -- so the same right-tail hold mechanism fires proportionally in quiet and violent years. Separately, expose volexp_range_atr / volexp_ibs / volexp_volume as tunable parameters so the volexp gate can be calibrated instead of only running at its defaults (2.0 / 0.8 / 3.0).

**Reasoning** The diagnosis is that a fixed 3% morning bar is a different rule in a quiet year than a violent one: 2026 produced no 3% mornings, so the entry condition was refused 319,662 times and the config took 3 trades for +0.05%, against 24 trades and +5.05% at 1.5%. The briefing's own conclusion is that the durable fix is a bar measured in units of each asset's own volatility rather than absolute percent. Code confirms itsm compares close/open-1 to an absolute itsm_threshold (momentum.py:406) with no volatility term, whereas volexp gates on range_vs_atr -- bar range in ATR units -- so its trigger self-scales with each year's realized volatility. volexp is the only vol-normalized entry that already exists in code and has never appeared in the ledger; donchian (the other alternative rule) was tried and took 0 trades, so testing whether the ATR-normalized rule instead maintains frequency is the informative next measurement. Lowering itsm_threshold to 1.5% is not proposed: it is a ledger repeat and still an absolute bar, so it does not address the regime-dependence root cause.

## 2026-08-13T01:45:32+00:00 · iteration 169

**Claim** An itsm entry whose 06:00 morning bar is measured as a multiple k of each asset's own trailing daily-return sigma (over volatility_window_days), instead of a fixed absolute itsm_threshold, will hold yearly trade count within a stable band across quiet and violent regimes -- firing a comparable number of times in a low-range year like 2026 as in 2018/2021 -- while preserving the monotone right-tail excess-over-drift the fixed-percent bar showed in-sample.

**Needs built** The itsm entry must compute its morning bar as k * the asset's trailing daily-return sigma (or daily ATR%) over volatility_window_days, rather than comparing the raw morning return to an absolute itsm_threshold. Today itsm_threshold is an absolute percentage and volatility_quantile/volatility_window_days only gate on a volatility percentile. Add a normalized-threshold variant -- a flag on entry_rule='itsm' (or reinterpret itsm_threshold as the sigma-multiple k when volatility_window_days>0) -- and expose k in a testable range (~0.5 to 4.0). Log per-year trade counts so the stable-frequency claim and the kill condition can be checked without spending the 2026 window.

**Reasoning** The sealed forensics are explicit: threshold 3.0% trained best of everything (+182% at 23.5%, survived eight years) yet produced 3 trades and +0.05% in 2026, against 24 trades and +5.05% for 1.5%. The entry condition was refused 319,662 times and the trend filter only 10 -- the rule did not stand aside in a bear market (it traded 34x in 2018, 31x in 2022); a quiet 2026 simply never printed a 3% morning move. A fixed absolute bar is therefore a DIFFERENT rule in each regime, and its selectivity drifts with ambient volatility. A bar in sigma units holds selectivity constant by construction. The existing volatility_quantile/volatility_window_days cannot deliver this: the ledger shows they only gate on a volatility percentile and thus REDUCE count (0.7/10 -> -0.4%; 0.85/20 and 0.6/20 froze the book at ~1% exposure). The normalization must live inside the threshold itself, not beside it as a filter.

## 2026-08-13T01:52:06+00:00 · iteration 171

**Claim** Dropping the absolute morning bar to its floor and letting a 5-day per-asset volatility quantile be the binding entry gate will keep the book invested in calm regimes where the fixed 3% bar collapsed to 3 trades in 2026, holding trade count near the 1.5%-threshold level (~24/yr) while staying more selective per trade than a flat 1.5% bar; a short window is the specific fix for the standstill the two prior 20-day runs hit.

**Needs built** The durable version expresses itsm_threshold in units of each asset's trailing realized daily volatility (k * sigma) rather than as a separate quantile gate layered on an absolute percent, so the 06:00 bar self-scales across quiet and violent years within the entry rule itself instead of relying on a bolt-on filter.

**Reasoning** The settled forensics say a fixed 3% bar is not a stable rule: 319,662 entry refusals in the quiet 2026 vs 34 trades in violent 2018. The lab has tried the vol params only at window=10 (as an overlay on thr=0.03, 252 trades, lost) and window=20 (thr<=0.005, book stood still twice). A 20-day quantile keeps the bar elevated through calm stretches that follow volatility, which is exactly why exposure went to ~1%. A 5-day window makes the bar track recent typical range, so it drops in a quiet year and a moderate morning move clears it — the adaptivity the fixed bar lacks. This is an information-gaining probe: if a short window restores exposure the quantile is return-relative and tunable; if it still stands still the mechanism must be built as a true per-asset sigma bar.

## 2026-08-13T02:09:01+00:00 · iteration 174

**Claim** On the five-major universe, itsm_threshold=0.015 scored on initial-capital drawdown (the incumbent's own basis) will beat the incumbent's 7.76 and, unlike threshold 0.030, keep enough trades in quiet years to stay measurable in a sealed window -- because 2026 already paid +5.05% over 24 trades at 1.5% against +0.05% over 3 trades at 3.0%, and initial-basis drawdown for this book is smaller than the 24.6% it printed on peak-basis.

**Needs built** Express the entry bar in units of each asset's own recent volatility instead of absolute percent: enter when the 06:00 UTC intraday move exceeds k x (ATR% or rolling stdev of daily returns over volatility_window_days), with k the tunable. The existing volatility_quantile is a regime gate, not a per-asset normalizer -- stacked on the absolute threshold it froze the book to ~1% exposure (two ledger rows) rather than firing a comparable trade count in quiet and violent years. This is the mechanism the 2026 forensics point to and it cannot be built from the current parameters.

**Reasoning** The lab has already spent a sealed window proving the incumbent's edge: threshold 3.0% trains best of everything (+182% at 23.5%) yet produced 3 trades and +0.05% in 2026 because a fixed 3% morning bar is a different rule in a quiet year -- the entry was refused 319,662 times, the trend filter only 10. Training score anti-predicts the forward window here. The 1.5% bar is the forward-validated one (+5.05%, 24 trades) but only exists in the ledger on drawdown_basis=peak (score 5.42, 24.6% drawdown -- against the abort line); re-scoring it on the incumbent's initial basis is untested and lets it replace the incumbent on like-for-like accounting rather than looking cosmetically worse. This is the honest interim step while the durable fix is queued below; it is not a repeat of any ledger row (no 0.015 config has drawdown_basis=initial with these exits).

## 2026-08-13T02:13:10+00:00 · iteration 175

**Claim** Switching the entry to volexp (a bar ≥ 2 ATR wide closing in its top quintile on ≥ 3× volume) — an entry bar scaled to each asset's own ATR rather than an absolute percent — will fire a comparable, double-digit number of trades in EVERY training year including quiet ones, curing the fixed-3% bar's collapse from 34 trades in 2018 to ~3 in 2026, while keeping positive return within the 25% mandate.

**Needs built** volexp's ignition knobs (volexp_range_atr / volexp_ibs / volexp_volume) are hardcoded, not exposed as tunables, so this test runs them at the 2.0/0.8/3.0 defaults. If volexp proves too coarse or too selective, the targeted durable build is an ATR/NATR-normalized itsm_threshold — buy at 06:00 when the morning move exceeds k× the asset's trailing typical daily range — which keeps the proven monotone morning-momentum dose-response (−0.40% at 0% … +2.49% at 3%) while making the bar mean the same thing in a quiet year and a violent one. That variant cannot be expressed today because itsm_threshold is absolute percent only.

**Reasoning** The briefing's own forensics settle that threshold-3% trains best (+182%/23.5%) yet made only 3 trades / +0.05% in 2026 because a fixed percent is a different rule across volatility regimes — the entry was refused 319,662 times, the trend filter only ten. Every absolute-percent lever is now exhausted (stops, trails, smaller sizing, more positions, wider universe, signal-scale, and thresholds 1.5–4.0% are all in the ledger). volexp is the one entry already served (range_vs_atr, internal_bar_strength, volume_ratio_20) that is genuinely volatility-relative, so it probes the briefing's stated durable fix at zero code cost. I keep the incumbent frame otherwise (stop_atr 60 = effectively no stop, trail 0, ~3-day hold, 3 positions, five-universe, veto off) so the ONLY variable under test is the entry rule.

## 2026-08-13T02:20:28+00:00 · iteration 177

**Claim** The fixed itsm_threshold is regime-dependent, so the durable fix is an entry bar in per-asset volatility units: at 06:00 UTC, enter when (prior-close→06:00 return) / trailing ATR% exceeds a tunable multiple k, with k set to the incumbent's ~top-decile selectivity. I expect this to fire a roughly regime-independent number of trades — double digits per year even in quiet years like 2026, where the fixed 3% bar fired 3 — while preserving the positive right-tail excess over drift.

**Needs built** A per-asset volatility-normalized entry threshold. At itsm_hour, for each symbol compute trailing ATR% (or realized vol) over volatility_window_days, then enter when (return from prior close to itsm_hour) / ATR% exceeds a tunable multiple k (suggest exposing k as a float, ~0.5–4.0, replacing itsm_threshold's role). This makes the bar auto-scale down in quiet regimes and up in violent ones so selectivity stays constant across years. It is distinct from the existing volatility_quantile, which gates on volatility REGIME and freezes the book — that param should not be reused for this.

**Reasoning** The lab already spent a sealed window proving the point: threshold 3.0% trains best (+182% at 23.5%) yet produced 3 trades and +0.05% in 2026 against 24 trades and +5.05% for 1.5%, and the forensics show the entry was refused 319,662 times because 2026 simply produced no 3% morning moves — the trend filter refused only 10. A fixed absolute percent is a different rule in a quiet year than a violent one. Lowering the bar is not the fix (excess over drift is monotone in the threshold: -0.40% at 0.0%, +2.49% at 3.0% — the edge IS the selectivity), so the mechanism must keep the SAME selectivity while measuring it relative to each asset's current volatility. The existing volatility_quantile/volatility_window_days params do not implement this: every configuration using them froze the book (1.01%, 0.98% average exposure) or went negative (-0.4%, 252 trades), which is the signature of a volatility-REGIME gate (trade only when realized vol is in the top quantile of history), not a per-trade vol-normalized entry bar. This is time-series momentum normalized by realized volatility, the standard construction in Moskowitz-Ooi-Pedersen; it needs code, and per the briefing that is the correct outcome for a genuinely new mechanism rather than burning another sealed window on the flaky quantile gate.

## 2026-08-13T02:23:40+00:00 · iteration 178

**Claim** Replacing the fixed absolute morning-move bar (itsm_threshold) with the volatility-relative 'volexp' entry — fire when the move clears the 0.70 quantile of each asset's own trailing 20-day move distribution — keeps the trade count in a stable band across quiet and violent years, trading materially more than the 3 trades the fixed 3% rule managed in 2026, while preserving the right-tail edge, because the bar now self-scales to each year's realized volatility instead of demanding an absolute 3% mo

**Needs built** Only if 'volexp' does not gate on itsm_hour — i.e. if it fires on any-hour volatility expansion rather than normalizing the 06:00 morning move by trailing ATR. The mechanism this hypothesis actually wants is the H-INTRA-002 morning-momentum bar expressed as a multiple of each asset's own recent volatility (06:00 move / trailing ATR > k). If the served 'volexp' rule is a generic intrabar breakout, that specific vol-normalized morning-momentum entry rule needs to be built and this run tests only the nearest available proxy.

**Reasoning** The hardest recorded lesson is that threshold 3.0% trains best yet made 3 trades / +0.05% in 2026 because a fixed 3% morning bar is a *different rule* in a quiet year than a violent one — the entry was refused 319,662 times while the trend filter fired only ten. The briefing names the durable fix explicitly: measure the bar in units of each asset's own volatility. 'volexp' is the only entry_rule never tested (itsm has dozens of rows, donchian one dead row) and is the natural home for that mechanism. The three prior vol-quantile rows all failed because they *layered* a vol filter on top of a fixed itsm threshold and over-selected ('the book stood still' at q=0.85; -0.4% at q=0.7 window=10 with the 3% bar still binding). Making the vol quantile the primary entry rule removes that double gate. q=0.70 sits below the q=0.85 that stood still and matches the ~firing rate of the itsm-0.015 config that took 24 trades / +5.05% in the sealed 2026 window; window=20 gives a stable per-asset estimate. Everything settled is preserved: stop_atr=60 and trail_atr=0 (no truncation of the right tail), maximum_positions=3, five-name universe, hold ~3 days (864 bars), risk 0.05.

## 2026-08-13T02:42:18+00:00 · iteration 182

**Claim** Replacing the fixed-percent morning bar with entry_rule='volexp' -- a breakout gauged against each asset's own recent volatility (window 14d) rather than an absolute 3% move -- keeps annual entry counts roughly stationary across the eight training years (no year in single digits) while holding a positive edge, precisely where the fixed 3% bar collapsed from ~30 trades/yr in violent years to 3 trades in quiet 2026.

**Needs built** Contingent fallback: if volexp turns out to be non-directional (buys downside volatility spikes) or returns ~0 trades like donchian did, then the mechanism must be built explicitly -- express the ITSM entry threshold as k times each asset's trailing ATR (or rolling daily-range over volatility_window_days) instead of an absolute percent, retaining the 06:00 UTC timing and the long-only up-move direction, so a single k fires comparably in quiet and violent regimes.

**Reasoning** The settled forensics say the incumbent's failure was not standing aside in a bear market (it trades 34x in 2018, 31x in 2022) but that a fixed 3% bar is a different rule in a quiet year -- 2026 produced almost no 3% morning moves, so the ENTRY condition was refused 319,662 times while the trend filter refused only 10. The briefing names the durable fix outright: a bar in units of each asset's own volatility. Lowering the absolute threshold (0.015) is already in the ledger and stacking volatility_quantile as a filter only starved trade count (all such runs negative or too-selective). entry_rule='volexp' is the one untested lever whose construction is inherently per-asset-volatility-normalized, and it must be paired with trend_ma_days=30 so it buys UP-side expansion in an uptrend, not downside vol spikes. Non-truncating exit geometry (stop_atr=60, trail_atr=0) is preserved because truncating the winner is settled to cost money.

## 2026-08-13T02:54:37+00:00 · iteration 185

**Claim** Replacing the fixed absolute morning bar with a volatility-relative one — the ITSM entry floored at 0.2% but gated by the 60th percentile of each asset's own recent (14-day) morning-move distribution — keeps trade count stable across calm and violent training years (no year collapsing more than ~2x below the median year's count) while holding training score within range of the threshold-1.5% config, whereas the fixed 3% bar collapsed to 3 trades in the quiet 2026 window.

**Needs built** Contingency only: if this run shows the leanest-year trade count still collapsing (killing the hypothesis), it means volatility_quantile behaves as a realized-volatility REGIME filter (trade less when calm) rather than a per-signal relative bar. The durable mechanism to build is then a genuine volatility-normalized entry: fire when (open-to-06:00 return) / (N-day ATR%) exceeds a constant k, so the bar auto-scales to each asset's current volatility with no absolute-percent floor at all.

**Reasoning** The diagnosis is explicit: a fixed 3% morning bar is a different rule in a quiet year than a violent one — in 2026 the entry was refused 319,662 times and the trend filter only 10, so threshold 3.0% made 3 trades/+0.05% while 1.5% made 24 trades/+5.05%. The stated durable fix is a bar in units of each asset's own volatility, not absolute percent. Every volatility_quantile run in the ledger failed, but ALL of them are in the over-selective regime — q=0.85/w=20 stood the book still, q=0.65/w=7 gave 4 trades in its leanest year, q=0.7/w=10 was on the wide universe with the 3% floor still binding. The moderate-quantile region with the absolute floor removed (threshold at its minimum so the volatility percentile is the sole binding gate) on the five-asset universe is untested and is exactly the mechanism the forensics recommend. Tuning q=0.6 targets the +0.5-to-+1.0% excess-over-drift zone — the same edge band as the forward-robust 1.5% bar — while the percentile reference rescales automatically when daily ranges shrink, which is the property the fixed bar lacked.

## 2026-08-13T03:02:46+00:00 · iteration 187

**Claim** Replacing the absolute 3% morning bar with a volatility-expansion entry (entry_rule='volexp'), which measures each asset's move against its own recent realized volatility rather than a fixed percent, will fire a comparable number of trades in BOTH violent and quiet regimes -- specifically it will produce a healthy trade count (>=20/year including its leanest year) instead of the 3-trade collapse the fixed 3% bar suffered in 2026 -- while preserving a positive, right-tail-driven training return a

**Needs built** Only as a fallback: if volexp turns out NOT to be a per-asset, sigma-normalised morning-momentum entry -- e.g. it ignores itsm_hour, or keys off index-level rather than each asset's own realized volatility -- then the genuinely new mechanism to build is an itsm variant whose entry bar is `today's session return > k * trailing realized sigma of THAT asset` (k a tunable expressed in vol units, sigma over volatility_window_days), replacing the absolute itsm_threshold. Queue that for a human if this run shows volexp is not vol-relative in the required sense.

**Reasoning** The diagnosis is explicit: threshold 3% trains best (+182% at 23.5%) but returned +0.05% on 3 trades in 2026 not because it stood aside in a bear market (it trades 34x in 2018, 31x in 2022) but because a fixed 3% morning bar is a DIFFERENT rule in a quiet year than a violent one, and 2026 produced no 3% morning moves -- the entry was refused 319,662 times, the trend filter only 10. The briefing names the durable fix directly: a bar measured in units of each asset's own volatility rather than absolute percent. entry_rule='volexp' (volatility expansion) is the one untested mechanism in the choice set that plausibly implements exactly this, and unlike the ledger's vol-quantile runs it is NOT an extra filter stacked on the absolute itsm bar (those double-gated and collapsed to 1-4 trades/leanest-year, score -0.80) -- it replaces the absolute bar. This is a training-run experiment, not a sealed-window spend, so trade-count stability across 2018/2022/2024/2025 is measured before any 2026 seal is considered. donchian gave 0 trades so volexp is the only live untried entry. I keep every settled-robust parameter fixed (hold 864, no stop stop_atr=60/trail=0 since truncating winners is proven costly, 3 positions since 6 breaches the budget, universe='five' since 'wide' costs drawdown, trend_ma_days=30) so the entry rule is the only variable.

## 2026-08-13T03:13:15+00:00 · iteration 189

**Claim** Swapping only the entry rule from absolute-percent itsm to the volatility-normalized volexp (range-expansion measured in ATR units), with all money-management held identical to the incumbent, produces era-stable trade frequency where itsm@3% did not: no calendar year collapses to near-zero trades (in particular the quiet 2023-2025 years each place a materially non-trivial count), while max drawdown stays under the 25% mandate.

**Needs built** Not for this test -- volexp already exists. If the thesis holds, the durable follow-up (queued for a human) is to preserve the documented itsm morning-momentum edge but make it scale-invariant: add an itsm variant whose trigger is close/opened-1 >= k * (rolling daily NATR) instead of a fixed itsm_threshold, exposed as a new parameter (e.g. itsm_threshold_atr). volexp confirming the direction is what justifies building that.

**Reasoning** The incumbent's failure is diagnosed: a fixed 3% morning bar is a different rule in a quiet year than a violent one -- 2026 refused the entry condition 319,662 times and it traded 3 times for +0.05%, while itsm@1.5% traded 24 times for +5.05%. The briefing names the durable fix as a bar measured in each asset's own volatility, not absolute percent. Code inspection (momentum.py:398-406) confirms itsm compares close/opened-1 to a fixed constant, and volatility_quantile (momentum.py:500-506) is only an additional veto -- neither scales the bar. But volexp (momentum.py:410-418) already triggers on range_vs_atr >= volexp_range_atr, i.e. in ATR units, so it is scale-invariant and fires at roughly constant frequency across eras. It is untried. Changing ONLY entry_rule isolates the entry mechanism from the money-management that is already tuned and settled, so the result cleanly measures whether a vol-normalized entry cures the era-dependence before a human is asked to build one for itsm.

## 2026-08-13T03:27:49+00:00 · iteration 193

**Claim** With the absolute morning-move floor dropped to its minimum (itsm_threshold=0.002) so the volatility-quantile gate is the SOLE entry filter, the ITSM edge fires at a regime-stable rate — at least ~15 trades in the leanest historical year and comparably in violent ones — while keeping training score positive, because a top-quartile move in each asset's own 14-day move distribution proxies the momentum tail independently of the absolute volatility level, which is exactly what a fixed 3% bar failed

**Needs built** An ATR-normalized morning-move entry: buy when the 06:00 UTC move-since-daily-open exceeds K * ATR(window) for that asset, replacing the fixed itsm_threshold with a per-asset volatility-scaled bar (K becomes the tunable). ATR is already computed for stop_atr/trail_atr, so the input exists. This differs structurally from volatility_quantile, which compares the PARTIAL-day 06:00 move against a distribution that makes high quantiles unreachable (the 0.85 book-standstill) and low quantiles edge-less — a direct ATR multiple on the same partial-day move sidesteps both and is the literal 'bar measured in units of each asset's own volatility' the diagnosis calls for.

**Reasoning** The forensics are explicit: 3.0% trained best (+182%/23.5%) yet made 3 trades and +0.05% in 2026 because 2026 produced no 3% morning moves, while 1.5% made 24 trades and +5.05% — a fixed absolute bar is a different rule across volatility regimes. The three volatility_quantile attempts already in the ledger all FAILED for one shared, diagnosable reason: the absolute itsm_threshold floor was still binding (0.7/10 kept 0.03 and went -0.4%; 0.65/7 kept 0.015 and collapsed to 4 trades in its leanest year exactly like the absolute bar; 0.85/20 stood still). None tested the quantile gate with the absolute floor actually removed. This genome isolates that one untested variable: threshold at floor, quantile 0.75, window 14, universe five, holding 864 bars and stop/trail untouched since truncating the right-tail winner is settled-costly. It is the cheapest in-parameter test of the briefing's own thesis and will either dominate or cleanly refute the quantile mechanism.

## 2026-08-13T03:31:06+00:00 · iteration 194

**Claim** Replacing the absolute 06:00-UTC morning-move bar with a volatility-normalized one — buy when the day's return-so-far exceeds k times each asset's trailing daily realized volatility (k≈1.0, estimated over volatility_window_days) — will hold trade count roughly stable across quiet and violent years (order 20–35 trades/year, INCLUDING a 2026-shaped quiet window) while preserving the right-tail excess-over-drift, so it beats both the incumbent (3 trades, +0.05% sealed 2026) and threshold-1.5% forwa

**Needs built** Yes. A new entry mode (e.g. entry_rule='itsm_vol') where the trigger is `intraday_return_at_hour >= k * trailing_daily_realized_vol` per asset, with k a new tunable multiplier (~0.5–3.0, since it cannot live inside itsm_threshold's 0.002–0.1 absolute range) and the vol estimated over volatility_window_days. This is a genuinely new mechanism, not reachable by any current parameter — volatility_quantile gates on vol regime, it does not rescale the entry bar.

**Reasoning** The forensics on the incumbent's 2026 collapse are explicit: it was NOT regime-avoidance (that same config trades 34x in 2018, 31x in 2022) — it was that a fixed 3% bar is a different rule in a quiet year, 2026 produced no 3% mornings, and the entry was refused 319,662 times while the trend filter refused only 10. The edge is monotone in threshold (excess goes negative at low absolute thresholds: -0.40% at 0.0%, -0.10% at 0.5%), so simply lowering itsm_threshold trades edge for trades — that is settled. The existing volatility_quantile parameter is the WRONG tool and its every ledger appearance confirms it: it is a conjunction that only fires when realized vol is HIGH, so it stands aside precisely in the low-vol condition that defines 2026 — hence 'the book stood still' (1.01% exposure), '4 trades', '-0.4%'. The one mechanism that keeps a high bar measured in volatility units while shrinking it in absolute terms during quiet regimes is normalization by the asset's own vol — which the briefing itself names as the durable fix.

## 2026-08-13T03:38:50+00:00 · iteration 196

**Claim** Replacing the absolute morning bar with a volatility-normalized one (entry_rule='volexp': buy at 06:00 UTC when the same-day return exceeds k times each asset's trailing realized daily volatility over volatility_window_days, rather than a fixed 3%) will hold the annual trade count roughly regime-invariant — producing on the order of 20+ trades in a low-volatility forward window like 2026 where the 3% absolute bar produced only 3 — while preserving the monotone right-tail edge, because per Gao-Ha

**Needs built** A volatility-normalized entry path: entry_rule='volexp' must compare the 06:00-UTC same-day return against k * trailing_realized_daily_volatility(volatility_window_days) instead of against the absolute itsm_threshold, with k the tunable multiple. If 'volexp' already implements a morning-move / realized-vol comparison this reaches a backtest as-is; if it implements something else (e.g. a Bollinger/range-expansion breakout unrelated to the 06:00 anchor), then the new code is exactly that sigma-denominated intraday-momentum bar, and the k multiple should be exposed in place of (or alongside) itsm_threshold so the firing rate can be calibrated to regime-invariance.

**Reasoning** The lab's own forensics named the durable fix: a bar in units of each asset's volatility rather than absolute percent, because 3% is a different rule in a quiet year (319,662 entry refusals, 3 trades in 2026) than a violent one (34 trades in 2018). Gao, Han, Li & Zhou, 'Market Intraday Momentum' (Journal of Financial Economics 2018, SSRN 2440866) show the intraday-momentum edge strengthens with volatility — meaning the predictive object is the move-to-vol ratio, which a sigma-denominated threshold holds constant across regimes. This is the OR/ATR normalization standard in opening-range work (e.g. Build Alpha / TradingView OR-to-ATR). Crucially it is NOT the refuted volatility_quantile gate: that added a high-vol filter on top of an already vol-selective percent bar (redundant selection → 252 trades and -0.4%, or a book that stood still at 1% exposure). Normalizing the bar instead of gating keeps firing rate stable while lowering the bar in quiet years — the exact failure mode of the sealed 2026 run. entry_rule='volexp' is untried in the ledger (only itsm and a 0-trade donchian have run), so the result changes what the lab believes whichever way it falls.

## 2026-08-13T04:18:43+00:00 · iteration 205

**Claim** A morning bar set at a fixed percentile of each asset's OWN recent 6-hour-return distribution (volatility_quantile≈0.6 over a 14-day window), REPLACING the absolute itsm_threshold in the entry test rather than gating on top of it, will fire a roughly regime-constant number of trades per year and so, unlike the fixed 3% bar, will not collapse to a handful of trades in a quiet year: I expect ≥15 trades in the leanest historical year while preserving positive excess-over-drift per trade on non-over

**Needs built** Yes. Change how volatility_quantile composes with the entry: instead of requiring (morning_return > itsm_threshold) AND (a separate volatility gate), make the entry fire when the 00:00→06:00 UTC return exceeds the volatility_quantile-th percentile of that asset's own 00:00→06:00 returns over the trailing volatility_window_days, with itsm_threshold dropped from the ITSM entry (or floored as a non-binding sanity minimum). The current build stacks the two conditions, which is what refuted the idea three times; the fix is substitution, not addition.

**Reasoning** The 2026 forensics are explicit that the killer was trade STARVATION, not regime: the 3% entry was refused 319,662 times while the trend filter refused only 10, giving 3 trades / +0.05% versus 24 trades / +5.05% at 1.5% — a fixed absolute morning bar is simply a different rule in a quiet year than a violent one. The three prior volatility_quantile runs all failed for the same structural reason: they AND the quantile onto the absolute itsm_threshold, compounding selectivity ('too selective', 'book stood still', 1% exposure) — the exact opposite of what is needed, since too-few-trades is the demonstrated failure mode. The durable fix the briefing itself names — a bar in units of each asset's own volatility — keeps expected trade frequency stationary across regimes by construction, which is precisely why time-series-momentum and breakout literature normalize signals by realized volatility (Moskowitz–Ooi–Pedersen; volatility scaling). This is a distinct mechanism, not a re-spin of the refuted stacked filter.

## 2026-08-13T04:42:24+00:00 · iteration 211

**Claim** The 2026 collapse was an entry-frequency failure of a fixed absolute bar, not a loss of edge; a volatility-expansion entry (entry_rule='volexp', bar sized in units of each asset's own recent volatility) will hold trade count roughly stable across regimes — where the fixed 3% bar fired 34x in 2018 but only 3x in quiet 2026 — so its leanest training year still books >=15 trades and its busiest/leanest ratio stays under 5x, while training return stays positive and drawdown stays under 25%.

**Needs built** None if 'volexp' already measures the entry bar in ATR/volatility units. If the backtest reveals volexp is not volatility-normalized (e.g. it collapses to 0 trades like donchian, or ignores volatility_window_days), the follow-up to queue for a human is an ATR-unit variant of the itsm bar: fire when the 06:00 up-move exceeds k x trailing per-asset ATR rather than a fixed percent.

**Reasoning** The sealed window already spent on threshold=0.03 is the decisive datum: 319,662 entry refusals vs only 10 trend-filter refusals, 3 trades and +0.05%, against 24 trades and +5.05% for the 1.5% bar. The briefing's own conclusion is that a fixed % morning bar is a different rule in a quiet year than a violent one, and the durable fix is a bar in each asset's own volatility units. 'volexp' is the ONLY entry_rule never tested (itsm dominates the ledger; the single donchian run took 0 trades) and is the natural in-code implementation of that fix. I am explicitly abandoning, not deepening, the volatility_quantile direction: all three vol-quantile runs (0.7/wide -0.4%, 0.65/five 4-trades-leanest, 0.85/0.005 'book stood still') failed because that parameter ADDs a restrictive regime gate ANDed onto the absolute threshold — it can only subtract trades — whereas the fix must re-express the threshold's units, not filter on top of it. Genome keeps the incumbent's proven skeleton (five, 3 positions, risk 0.05, stop_atr 60 = effectively no stop per the settled right-tail finding, trail 0, trend_ma 30, 864-bar/3-day hold) and changes only the entry mechanism, with volatility_window_days=14 and a moderate volatility_quantile=0.6 to avoid the 0.85 book-freeze.

## 2026-08-13T04:45:10+00:00 · iteration 212

**Claim** Replacing the absolute-percent morning bar with a volatility-expansion entry (entry_rule='volexp'), whose trigger scales to each asset's own recent range via volatility_window_days=14 and a permissive volatility_quantile=0.5, will keep trade count roughly stable across quiet and violent years — specifically it will fire dozens of times in a low-range year rather than collapsing to single digits — while producing a score within a factor of two of the +7.76 incumbent.

**Needs built** Only if the existing 'volexp' entry_rule is NOT a range-normalized breakout. The falsifiable test assumes volexp fires when the current bar's move exceeds k times a recent-range measure (volatility_window_days) rather than an absolute percent. If volexp instead means something else (e.g. an absolute volatility-of-returns filter layered on itsm), then a true Crabel-Stretch entry must be built: at itsm_hour, place the buy at daily_open + k * mean_{N days}(|open - nearest_extreme|), with N = volatility_window_days — a genuinely new mechanism worth queuing.

**Reasoning** The hardest settled result in the briefing is that a fixed 3% morning bar is a DIFFERENT rule in a quiet year than a violent one — 2026 simply did not produce 3% morning moves, so the entry was refused 319,662 times and 3 trades resulted (+0.05%), against 24 trades / +5.05% for the 1.5% bar. The briefing names the durable fix explicitly: a bar measured in units of each asset's own volatility rather than absolute percent. Toby Crabel's 'Stretch' construct is exactly this and predates crypto: the entry offset is set to the N-day average of the distance from the open to the nearest extreme, so the trigger self-rescales with the current volatility regime (Crabel, 'Day Trading with Short Term Price Patterns and Opening Range Breakout'; summarised at oxfordstrat.com/trading-strategies/opening-range-breakout). The 'volexp' entry_rule has never appeared in the ledger — only itsm and one dead donchian — so this is untested territory, not a deepening of itsm. I set volatility_quantile to its floor (0.5) deliberately: the prior quantile experiments failed by being too SELECTIVE (0.65–0.85 froze the book), the opposite of what a quiet-year rule needs. Everything else is held at the incumbent — hour 6, holding 864, no stop (stop_atr 60/trail 0, since truncating the right-tail winner is settled to cost money), 3 positions, trend_ma 30, five-name universe.

## 2026-08-13T04:48:13+00:00 · iteration 213

**Claim** Replacing the fixed-percent morning bar with the untested volatility-relative entry (entry_rule=volexp, 14-day vol window, median quantile 0.5, everything else at incumbent) decouples trade count from the era's absolute range: it fires >=15 trades in EVERY calendar year including the quiet 2023-2026, versus the 3 trades the fixed 3% bar produced in 2026, while holding a non-negative training score below the 25% mandate.

**Needs built** This genome is runnable (volexp exists), but if volexp turns out to be a generic intraday breakout rather than a rescaled morning-momentum bar, the truly durable fix is not expressible with current parameters: add an ATR-normalised itsm threshold (e.g. itsm_threshold_atr, morning move measured as a multiple of trailing realised vol / ATR) that preserves the 06:00 timing and 3-day hold but self-scales the entry bar per asset per regime. That is the exact mechanism the forensics call for and no listed parameter encodes it.

**Reasoning** The forensics already named the durable fix: a fixed 3% morning bar is a different rule in a quiet year than a violent one -- in 2026 the entry condition was refused 319,662 times purely because that year produced no 3% mornings, not because a trend filter stood aside (10 refusals). The settled cure is 'a bar measured in units of each asset's own volatility rather than absolute percent.' volexp is the only entry_rule the ledger has never touched (itsm everywhere, one donchian at 0 trades) and is precisely a volatility-relative entry, so it is the single-variable test of that diagnosis. I hold entry timing structure fixed to protect what is already proven: maximum_holding_bars=864 keeps the ~3-day hold, stop_atr=60/trail_atr=0 keep the winner un-truncated (stops and trails both shown to cost money), maximum_positions=3 and universe=five respect the settled drawdown budget. quantile=0.5 (median) is deliberately the least-selective setting to avoid the over-gating that made prior volatility_quantile runs at 0.65-0.85 stand still (1.01% exposure, 4 trades in the leanest year). itsm_threshold is pinned near the floor (0.005) so that if volexp reuses it as an absolute gate it stays non-binding and volatility_quantile remains the real knob -- otherwise we would silently re-import the fixed-percent collapse we are trying to kill.

## 2026-08-13T04:58:50+00:00 · iteration 216

**Claim** Replacing the fixed 3% morning bar with entry_rule='volexp' — buy the hour-6 move only when it exceeds the 0.60 quantile of that asset's own trailing 7-day move distribution — keeps the morning-momentum edge alive AND self-scales to quiet regimes: it will trade >=15 times per year in every year including 2026 (versus the 3-trade collapse of the absolute 3% bar) while producing a positive score at drawdown under the 25% mandate.

**Needs built** Only if entry_rule='volexp' does NOT already express the signed hour-6 morning move as a quantile of each asset's trailing N-day distribution of the same-window return — e.g. if it triggers on generic unsigned range/ATR expansion rather than a signed morning-up move gated by volatility_quantile over volatility_window_days. In that case the faithful construct must be built as an entry rule: signal = cumulative return since prior close at itsm_hour, fire when signal >= volatility_quantile of that asset's trailing volatility_window_days distribution of that same signal.

**Reasoning** The forensics on threshold-3.0% named the mechanism: a fixed 3% morning bar is a different rule in a quiet year than a violent one, and 2026 simply produced no 3% morning moves (entry refused 319,662 times, trend filter only 10). The durable answer the lab itself flagged is a volatility-relative bar. Crabel's Opening Range Breakout literature (LiteFinance/TradersMastermind summaries of Crabel's NR7 work) says the breakout pays precisely when the move is large relative to a recent contraction baseline, and Gao-Han-Li-Zhou 'Market Intraday Momentum' (SSRN abstract 2440866) shows the morning-predicts-later effect strengthens on more volatile days — both argue the trigger should be measured in the asset's own volatility units. entry_rule='volexp' is the only lever in the parameter set that does this and is untried in the ledger; the three existing volatility runs all AND-ed a quantile onto the 3% itsm bar (double-filtering that over-selected: q0.65/w7 left 4 trades in its leanest year), so they never tested the reframing itself. I hold every other proven parameter at the incumbent (itsm_hour=6, holding 864 bars, stop_atr=60 i.e. no truncation, trail 0, trend_ma 30, 3 positions, five-universe) so the only thing under test is the entry's unit of measure. Sources: SSRN 2440866 (Gao, Han, Li, Zhou); Crabel ORB/NR7 as summarized at litefinance.org and tradersmastermind.com.

## 2026-08-13T05:02:06+00:00 · iteration 217

**Claim** An itsm entry bar redefined as k times each asset's trailing 14-day volatility (ATR%), calibrated so k reproduces roughly a 1.5% bar in a median-vol year, will hold a comparable trade count (~20+/yr) and positive per-trade edge across BOTH quiet and violent regimes -- eliminating the failure where the fixed 3% absolute bar collapsed to 3 trades and +0.05% in 2026 while the 1.5% bar took 24 trades and +5.05%.

**Needs built** A volatility-normalized itsm entry rule: replace the absolute morning-move test (day_return >= itsm_threshold) with day_return >= k * trailing_volatility, where trailing_volatility is ATR% or realized daily vol over volatility_window_days, and k is carried in the itsm_threshold slot (reinterpreted as a coefficient, not an absolute fraction). Existing params (volatility_quantile, itsm_threshold) cannot express this because quantile gates on a percentile of realized vol (fewer trades) rather than rescaling the bar.

**Reasoning** The hardest settled fact here is that threshold 3.0% trains best (+182% at 23.5%, survives eight years) yet returned 3 trades / +0.05% in 2026, and the forensics say why: the ENTRY condition was refused 319,662 times against ten trend-filter refusals -- 2026 simply did not print 3% morning moves. That same config trades 34x in 2018 and 31x in 2022, so it is not standing aside in a bear market; a fixed absolute morning bar is literally a different rule in a quiet year than a violent one. The briefing itself names the durable fix: a bar measured in each asset's own volatility rather than absolute percent. This is NOT the existing volatility_quantile mechanism -- that is a selectivity FILTER, and every quantile config in the ledger (0.65, 0.7, 0.85) collapsed trade count or froze the book, the exact wrong direction. What is needed is normalization of the threshold itself so one coefficient auto-loosens when daily ranges shrink. Lowering the absolute threshold to 1.5% is the crude version that already beat 3.0% forward; normalizing it is the mechanism that should generalize, and this hypothesis is not in the ledger.

## 2026-08-13T05:18:13+00:00 · iteration 221

**Claim** Replacing the fixed-percent 06:00 bar (itsm) with the already-built volatility-normalized expansion bar (volexp: range ≥ 2×ATR, close in the top of the bar, volume ≥ 3× normal) keeps the leanest historical year's trade count in double digits and yields a positive, measurable sealed-2026 return, because an ATR-denominated bar self-scales to each year's volatility where the fixed 3% bar collapsed to 3 trades in quiet 2026.

**Needs built** The cleanest isolation — keep the known-good 06:00 momentum edge but denominate its trigger in NATR (buy if close/open-1 ≥ k·natr_14) rather than absolute percent — does not exist: itsm_threshold is a fixed float and the signal-scale keys off it (momentum.py:406,447). Separately, volexp's sub-thresholds (volexp_range_atr / volexp_ibs / volexp_volume) are not in the tunable set, so volexp can only be run at its 2.0/0.8/3.0 defaults; if that proves too selective it cannot be loosened. Expose an itsm-in-NATR-units entry and make volexp's three sub-parameters tunable.

**Reasoning** The settled hardest lesson is that a fixed 3% morning bar is a different rule in a quiet year than a violent one — in 2026 the entry was refused 319,662 times, not by the trend filter, so threshold 3.0% made 3 trades and +0.05% while 1.5% made 24 and +5.05%. The briefing names the durable fix explicitly: a bar in units of each asset's own volatility rather than absolute percent. That mechanism already exists in code as entry_rule='volexp' (momentum.py:410-418), which triggers on an ATR-wide bar closing on its high with a volume spike — by construction it fires in both quiet and violent regimes. It has never been measured (the ledger holds only donchian=0-trades and itsm), and it is a genuinely different lever from the volatility_quantile NATR veto that is already refuted three ways for making the book stand still. This is not a threshold-raise: it does not narrow itsm, it swaps the denominator. Lowering itsm_threshold to 1.5% is already known and is not proposed.

## 2026-08-13T05:22:29+00:00 · iteration 222

**Claim** Lowering the morning entry bar from an absolute 3.0% to 1.5% while holding the incumbent's structure (drawdown_basis=initial, trend_ma_days=30, five-universe, no stop) keeps the entry firing in low-range years, so a 2026-type trade-count collapse (3 trades, +0.05%) will not recur: expect >=20 trades and a positive return in the sealed 2026 window, at drawdown near the mandate edge.

**Needs built** The fully durable fix the briefing points at -- a morning bar expressed in units of each asset's OWN volatility (e.g. enter at 06:00 when the day's gain >= k * trailing-14d daily ATR%, k tunable) rather than in absolute percent -- has no tunable parameter. itsm_threshold is absolute; volatility_quantile gates on the volatility REGIME (top-X volatile periods), which is why every quantile attempt in the ledger (0.65/0.7/0.85) became too selective and returned <=-0.01 -- it does not normalize the bar, it just trades only in violent windows. Queue an ATR-normalized itsm entry rule ('gain in ATR units') so the fire-rate is invariant to whether the year is quiet or violent. That direction (quantile-as-regime-gate) should be abandoned, not deepened; the ATR-unit bar is the replacement.

**Reasoning** The 2026 forensics are unambiguous: the 3% bar was refused 319,662 times and traded 3 times because 2026 produced no 3% morning moves, while threshold-1.5% trades 24x for +5.05% on the same forensic replay. That 1.5% figure is a replay estimate, not a spent seal (no SEALED tag in the ledger), so this converts it into a real forward result. The specific combo threshold=0.015 + drawdown_basis=initial + trend_ma_days=30 is NOT in the ledger (all logged 1.5% runs use drawdown_basis=peak), so this is a new sealed test, not a repeat. It directly attacks the named failure mode: a lower absolute bar fires at a higher, more year-stable rate. I flag the honest limitation below: 1.5% is still absolute and would itself collapse in an even quieter year.

## 2026-08-13T05:25:17+00:00 · iteration 223

**Claim** Replacing the fixed absolute morning bar with a threshold expressed in units of each asset's own trailing volatility (enter when the 06:00 return exceeds k times its rolling daily-return sigma) will hold trade count roughly constant across quiet and violent years -- producing 20+ trades in 2026 rather than 3 -- while preserving the right-tail edge, because a self-scaling bar fires as often in a low-range year as in a high-range one.

**Needs built** A served entry rule that, at itsm_hour, computes the morning return as a multiple of each symbol's trailing realized volatility -- rolling standard deviation of daily returns over volatility_window_days -- and enters when that ratio exceeds a tunable k, replacing the absolute-percent comparison in the current itsm rule. k should map onto a served float (reusing itsm_threshold's slot, reinterpreted from percent to sigma-multiples, roughly 1.0-4.0). The existing volatility_quantile gates the volatility regime, not the size of the move relative to that regime, so it cannot substitute for this.

**Reasoning** The hardest settled finding, bought with a sealed window, is that threshold 3.0% is a DIFFERENT rule in a quiet year than a violent one: in 2026 the entry condition was refused 319,662 times and the trend filter only ten, and the same config trades 34 times in 2018 and 31 in 2022 -- so it did not stand aside in a bear market, it starved because 2026 produced no 3% morning moves. The briefing itself names the durable fix: a bar measured in units of each asset's own volatility rather than in absolute percent. This is not the served volatility_quantile: those three runs (quantile 0.7/0.65/0.85) all layered a volatility-REGIME gate ON TOP of the absolute itsm_threshold and either lost money (-0.4% at 0.7) or froze the book (1.01% exposure at 0.85) -- they compounded selectivity instead of replacing the absolute bar. Retreating to threshold 1.5% (+5.05%, 24 trades in 2026) is already measured and is a retreat, not a mechanism. The raise-the-bar direction should be abandoned; the correct next step is to make the bar dimensionless in sigma, which the served parameters cannot express.

## 2026-08-13T05:39:37+00:00 · iteration 227

**Claim** Because a fixed-% morning bar is regime-dependent (settled), lowering itsm_threshold one notch to 0.02 on the five-name universe — no stops, no volatility_quantile filter, initial drawdown basis like the incumbent — will fire 15–25 trades and return >+2% in the 2026 forward window, sitting between the two sealed endpoints (0.03 → 3 trades, +0.05%; 0.015 → 24 trades, +5.05%); the durable fix, a bar scaled by each asset's own trailing volatility rather than absolute percent, should instead hold tr

**Needs built** Add an 'itsm' entry variant in which the morning-move bar is k × the asset's trailing-N-day realized daily volatility (or ATR), replacing the absolute itsm_threshold, so the required move auto-scales per asset and per regime. This is distinct from volatility_quantile (which gates on the asset's volatility distribution and has been refuted three times); here the bar itself is expressed in volatility units, with k and the lookback window as the two tunables.

**Reasoning** The sealed forensics say the incumbent's failure was mechanical: in 2026 the entry condition was refused 319,662 times and the trend filter only ten — 2026 simply did not produce 3% morning moves, while the same rule traded 34× in 2018 and 31× in 2022. The settled lesson is that a fixed absolute bar is a different rule in a quiet year than a violent one. The existing knob for this, volatility_quantile, is refuted three times (score -0.01 with 252 trades at -0.4%, -0.80 too selective, -0.90 book stood still): as a bolt-on it either double-filters into starvation or, when it admits enough trades, destroys the right-tail edge — evidence it gates on the asset's vol REGIME, not on the size of the morning move the mechanism needs. The genuine fix scales the entry bar itself, which is not expressible in current parameters. 0.02 (initial basis, no deleverage) is the best available fixed stopgap and — unlike 0.015 and 0.03 — its 2026 window is unspent, so it cheaply brackets where the fixed-threshold frequency/edge tradeoff sits before we invest in code.

## 2026-08-13T05:49:20+00:00 · iteration 230

**Claim** Replacing the fixed absolute morning bar (itsm_threshold) with the volatility-expansion entry rule (entry_rule=volexp, gated on each asset's own trailing-volatility quantile) will fire at a comparable rate in quiet and violent years, so its leanest training year still books >=15 trades and its 2026 trade count lands in the 1.5%-threshold regime (20+) rather than collapsing to the 3 trades the fixed 3% bar produced.

**Needs built** Only if volexp is a generic volatility-expansion rule not anchored to the 06:00 momentum event. The precise durable fix is an ATR-normalized itsm: interpret itsm_threshold as a multiple of the asset's trailing ATR (e.g. morning return >= k * ATR14) rather than an absolute return, so the same k fires at the same frequency across regimes. If the volexp run is killed by low trade count, build that ATR-normalized morning bar.

**Reasoning** The sealed window already spent its verdict on the fixed bar: threshold 3.0% trains best (+182% at 23.5%) yet returned +0.05% on 3 trades in 2026, because the entry was refused 319,662 times while the trend filter refused only ten -- a fixed 3% morning move is a different rule in a quiet year than a violent one, and 2026 had no 3% mornings. The briefing itself names the durable fix as a bar in units of each asset's own volatility. Every volatility_quantile run in the ledger STACKED a quantile filter on top of the retained itsm_threshold=0.03 (doubly selective -> 4 trades in leanest year, or 1.01% exposure), which is not the same experiment. volexp is the one entry_rule never tested (donchian was, and returned 0 trades, confirming the harness does route entry_rule), and it is the only listed choice that expresses momentum in volatility units instead of absolute percent. This tests the diagnosed mechanism directly instead of re-deriving that fixed thresholds do not transfer.

## 2026-08-13T06:44:47+00:00 · iteration 269

**Claim** Replacing the absolute morning-move bar with one expressed in units of each asset's own trailing volatility — buy at 06:00 UTC when the open→06:00 return exceeds K·ATR(volatility_window_days), with K calibrated to reproduce the incumbent's ~345 training trades — will hold entry frequency roughly stationary across eras, yielding 20–35 trades in a quiet forward year (as the 1.5% bar did, 24 trades / +5.05% in 2026) while keeping the right-tail edge that the fixed 3% bar earned in training.

**Needs built** An ITSM entry variant whose trigger is a multiple K of trailing realized volatility rather than an absolute percentage: fire when (close_at_hour − open_of_day)/open ≥ K · ATR(volatility_window_days)/price, exposing K as the tunable (replacing itsm_threshold) and reusing volatility_window_days for the ATR lookback. It must REPLACE the absolute bar, not stack on it like the current volatility_quantile filter does. Everything downstream of entry (06:00 hour, ~3-day hold, no stop truncation, trend filter, 3 positions) stays as in the incumbent.

**Reasoning** The forensic result is decisive and already spent a sealed window: threshold 3.0% trains best (+182% at 23.5%) yet made 3 trades / +0.05% in 2026, and the entry condition was refused 319,662 times against only 10 trend refusals — while the SAME config traded 34 times in 2018 and 31 in 2022. The bar did not stand aside in a bear market; a fixed 3% morning move is simply a rarer event in a quiet year, so an absolute-percent bar is a different rule in every regime. The briefing names the durable fix explicitly: measure the bar in units of the asset's own volatility. Note the existing `volatility_quantile` param does NOT do this — it is a rank gate layered ON TOP of the absolute threshold, which is why it has been refuted three times (thr=0.03·q=0.7 → −0.4%; thr=0.015·q=0.65 → 4 trades in its leanest year; thr=0.005·q=0.85 → the book stood still): double-gating shrinks trade count instead of rescaling the bar. A continuous K·ATR threshold that REPLACES the absolute percent is the theoretically correct construction — this is volatility-scaled time-series momentum (Moskowitz, Ooi & Pedersen 2012), where signals are normalized by ex-ante volatility precisely to keep firing rate and risk contribution stationary across regimes. This is a new mechanism, not a tweak of a tunable, so it is queued rather than backtested this iteration.

## 2026-08-13T06:48:16+00:00 · iteration 270

**Claim** Replacing the fixed absolute morning bar with a volatility-normalized one -- fire the 06:00 entry when (close/open - 1) >= k * NATR_14 instead of >= itsm_threshold -- and calibrating k so the rule reproduces the fixed-3% trade count in the violent years (34 in 2018, 31 in 2022) will, unlike the fixed 3% bar, still produce a comparable trade count in quiet years, because a 3%-equivalent move in NATR units is a low absolute percent when daily ranges are small.

**Needs built** A new entry variant (e.g. entry_rule='itsm_natr', or a new tunable itsm_threshold_natr as a multiple of NATR) that gates the 06:00 itsm entry on (close/open - 1) >= k * natr_14 using the already-served natr_14 indicator, replacing the absolute-percent comparison at momentum.py:406. It must NOT reuse volatility_quantile, whose semantics are an opposite-signed veto. Provide k as a tunable float (suggested initial search 0.5-1.5) so it can be calibrated to reproduce fixed-3% violent-year trade counts before the quiet-year count is read.

**Reasoning** The forensics on the sealed 3.0% run already located the failure: the entry condition was refused 319,662 times in 2026 and the trend filter only ten, so the rule did not stand aside in a bear market -- it never saw a 3% morning move because 2026 was quiet. The briefing names the durable fix as a bar in units of each asset's own volatility. I confirmed in momentum.py that this is not implementable: itsm_threshold is absolute percent (line 406) and the only volatility parameter, volatility_quantile, is an elevated-NATR veto (lines 500-506) that filters out exactly the violent days the mechanism pays on -- which is why quantile=0.7/0.85 in the ledger scored -0.01 and stood the book still. Lowering the fixed threshold is settled (monotone edge decay; 1.5%'s behavior is already known), so there is no untried within-genome experiment that addresses the actual defect. NATR_14 is already served and observed, so this is a one-comparison change, not a new indicator.

## 2026-08-13T07:04:51+00:00 · iteration 275

**Claim** An itsm morning-momentum entry whose bar is self-scaling — the 06:00 up-move must clear the 75th percentile of each asset's own trailing 30-day morning-move distribution (volatility_quantile=0.75, volatility_window_days=30), with the absolute floor relaxed to itsm_threshold=0.002 so the quantile is the sole binding gate — will fire ≥20 trades in every calendar year including quiet regimes, and hold a positive training score at <25% drawdown on the five-name universe.

**Needs built** If volatility_quantile gates on a quantile of the return distribution — which by construction always admits the top (1-q) fraction of moves regardless of their absolute size, so in a quiet regime it admits small moves that lack the right tail — rather than on morning-return ÷ trailing realized volatility, then the briefing's literal durable fix is not expressible and must be built: a distinct entry mode that fires when the 06:00 move ≥ k × the asset's trailing ATR/σ (k tunable ~1.0–3.0), which preserves absolute-magnitude selectivity while self-scaling across regimes. The genome above is the immediate probe; this is the mechanism it is a stand-in for.

**Reasoning** The sealed window already spent the verdict on a fixed bar: threshold 3.0% fired 3 times for +0.05% in 2026 because a quiet year printed no 3% mornings (entry refused 319,662 times, trend filter only 10), while 1.5% fired 24 times for +5.05%. The diagnosis names the durable fix explicitly — a bar in units of each asset's own volatility, not absolute percent. Every prior quantile attempt that failed was confounded: q=0.70/w=10 rode the *wide* universe (known drawdown tax) to -0.4%; q=0.65/w=7 paired a short noisy window with a competing 1.5% absolute floor and starved to 4 trades; q=0.85/w=20 was simply too tight and stood still. This proposal strips all three confounds — five-name universe, a long 30-day per-asset reference that reflects regime rather than a week's noise burst, and the quantile as the single binding gate — to test the briefing's hypothesis cleanly. It is not a repeat of any ledger row (the untested corridor between 'stood still' at 0.85 and 'traded-but-lost-wide' at 0.70). Everything else is held at the +7.76 incumbent: no stop, no trail (truncating the winner is settled to cost money), hold 864 bars, 3 positions, trend_ma_days=30.

## 2026-08-13T07:14:27+00:00 · iteration 278

**Claim** Replacing the fixed 3% absolute morning bar with a volatility-relative gate — a minimal absolute floor (itsm_threshold=0.005) plus a moderate own-asset percentile (volatility_quantile=0.55 over a 20-day window) — will hold trade frequency roughly stable across regimes, firing 25+ trades in every historical year including the lean 2023/2024-style ones, where the absolute 3% bar collapsed to single digits and went forward-dead (+0.05%, 3 trades) in 2026.

**Needs built** The genome above is the closest test buildable from existing parameters (assuming volatility_quantile gates the morning move against its own trailing distribution rather than filtering by realized-vol regime). The durable version is an itsm_threshold expressed directly in volatility units — entry when the 06:00 day-return exceeds k × the asset's ATR/realized-vol over volatility_window_days — so a single k is one rule in every regime. That requires a new vol-scaled threshold parameter (itsm_threshold_atr, ~0.3 to 3.0) replacing the absolute itsm_threshold in the itsm entry check. Queue it if the quantile approximation shows regime-stable trade counts.

**Reasoning** Absolute-threshold tuning is exhausted: 0.5/1.5/2/3/4/10% are all measured, and the forensics you handed me name the mechanism failure directly — 'a fixed 3% morning bar is a different rule in a quiet year than in a violent one', refused 319,662 times in 2026 while trading 34x in 2018. The incumbent trains best (+182%) yet sealed at a forward-dead +0.05%; threshold 1.5% fires (24 trades) and made +5.05% forward but sits at 24.6% drawdown, one step from the abort. The durable fix you point to is a bar in units of the asset's own volatility, not absolute percent. Prior vol-quantile runs failed only because they stacked a HIGH quantile (0.85/0.65) ON TOP of a high absolute floor (0.03), making entry doubly selective — the book stood still. Inverting that (near-zero floor, low 0.55 quantile, long stable window) has not been tried and directly answers the owed question 'how often does it fire when daily ranges are small' — by design, whenever the morning move beats the asset's own median-ish recent move, small ranges included. This lowers rather than raises the effective bar in quiet years, which is exactly where the absolute rule died.

## 2026-08-13T07:30:16+00:00 · iteration 283

**Claim** Replacing the absolute morning-move bar with a volatility-normalised entry (entry_rule='volexp', firing when the 06:00 move exceeds a multiple of each asset's own recent volatility rather than a fixed 3%) will keep trade count stable across quiet and violent years alike — it will fire enough in a low-volatility year to be measurable while preserving the right-tail edge — producing at least 15 trades in its leanest calendar year and a positive score.

**Needs built** If 'volexp' is implemented as a generic volatility-expansion breakout rather than specifically 'morning-move ÷ ATR > k on the 06:00 ITSM signal', then the exact durable fix — an ITSM entry whose threshold is expressed in units of each asset's trailing ATR/vol instead of absolute percent — still needs to be built as a first-class entry mode.

**Reasoning** The sealed-window forensics are explicit: threshold 3.0% trains best of everything (+182% at 23.5%, score +7.76) yet produced 3 trades and +0.05% in 2026, because a fixed 3% morning bar is a different rule in a quiet year than a violent one and 2026 produced no 3% mornings. The briefing names the durable fix directly — a bar measured in units of each asset's own volatility rather than absolute percent. Every fix already tried is exhausted or refused: stops/trailing truncate the paying right tail, smaller sizing draws the same curve, more positions and a wider universe cost drawdown, signal-strength scaling is refuted in three variants, and raising the threshold to 4.0% loses on trade-count collapse. The vol-quantile experiments that stacked a volatility gate ON TOP of the 3% bar failed for the opposite reasons (quantile 0.85/window 20 froze the book at 1.01% exposure; 0.7/window 10 kept 252 trades but bled the edge to -0.4%) — that is an additive over-filter, not a normalised bar. entry_rule='volexp' is the one unexplored entry mechanism (itsm is mined out, donchian took 0 trades) and is the natural home for a move-per-volatility entry, which auto-adapts the effective bar down in calm regimes like 2026 and up in violent ones — the exact adaptivity the fixed bar lacks.

## 2026-08-13T07:33:19+00:00 · iteration 284

**Claim** Replacing the fixed 3% morning-move bar with a sigma-scaled bar -- buy at 06:00 UTC when each asset's open-to-06:00 return exceeds k times the rolling standard deviation of its OWN open-to-06:00 return over the prior ~14 days (k tuned so 2016-2025 trade counts match threshold=1.5%, ~24/yr) -- will hold trade count roughly constant across quiet and violent years and, on the sealed 2026 window, will fire on the order of 20+ times (versus 3 for fixed 3%) while matching or beating threshold=1.5%'s +

**Needs built** A new entry mode where itsm_threshold is interpreted as a multiple k of the per-asset rolling standard deviation of the open-to-itsm_hour return, estimated over volatility_window_days (e.g. 14) of prior days, instead of an absolute return fraction. Requires: (1) per-asset, per-bar computation of the open-to-06:00 return series and its trailing std over the lookback window; (2) entry test 'today's open-to-06:00 return >= k * that std' rather than '>= itsm_threshold'; (3) exposing k as a tunable (suggest range ~0.75 to 2.5). Everything else (hold 864 bars, no stop, trend_ma 30, five universe, positions 3) stays as the incumbent so the change is isolated to the entry denomination.

**Reasoning** The lab's own hardest lesson is that a fixed 3% morning bar is a different rule in a quiet year than a violent one: in 2026 the entry was refused 319,662 times (not the trend filter), because 2026 produced no 3% morning moves, and the briefing names 'a bar measured in units of each asset's own volatility' as the durable fix. Zarattini, Aziz & Barbon, 'Beat the Market: An Effective Intraday Momentum Strategy for S&P500 ETF' (SSRN 4824172, 2024), build entries off a 'noise area' whose width is the volatility of open-to-T intraday returns estimated from historical same-time-of-day moves -- an entry threshold that self-scales to the current volatility regime and to intraday seasonality, holding signal difficulty constant across calm and turbulent periods. This is distinct from the volatility_quantile runs already in the ledger, which stacked a quantile FILTER on top of the fixed threshold and starved the book (4 trades/yr); this proposal REPLACES the fixed threshold with the sigma-scaled one rather than adding a second gate. It is the single lead that would change what the lab believes: it isolates whether threshold-3%'s 2026 failure was the denomination of the bar or the death of the anomaly.

## 2026-08-13T07:37:36+00:00 · iteration 285

**Claim** Replacing the fixed absolute morning bar with a threshold expressed in units of each asset's own trailing realized volatility (fire when the 06:00 UTC day-return exceeds k times the N-day realized daily vol) will fire a comparable number of trades in a quiet year and a violent one — closing the gap between the 34 trades the fixed 3% bar took in 2018 and the 3 it took in 2026 — while preserving the right-tail edge that the fixed 3% bar showed in training.

**Needs built** Add a vol-normalized entry mode to entry_rule='itsm': instead of comparing the 06:00 UTC intraday return to a constant itsm_threshold, compare it to k * sigma, where sigma is the trailing N-day realized daily volatility of that same symbol (N = volatility_window_days, already served) and k is a new tunable (reuse itsm_threshold's slot as k, roughly 0.8–2.0, or add itsm_threshold_vol_k). This keeps the mechanism (buy strength at the open, hold the right tail) but makes 'strength' regime-portable per asset. Pair a training run and a 2026 run with identical params except trade_from so the monitor can measure trade count and return side by side against the absolute-bar baseline.

**Reasoning** The one sealed measurement we own says the incumbent's failure was not the trend filter (it refused only 10 times in 2026) but the entry condition (refused 319,662 times): a fixed 3% morning bar is simply a rarer event in a low-range year, so the rule that trained best went nearly flat forward. Sizing, position count, universe width, stops/trails, signal-scaled risk, and raising/lowering the absolute bar are all settled or refuted, and every one leaves the threshold denominated in absolute percent — the exact quantity that drifts with the vol regime. The briefing itself names the durable fix as a bar in units of the asset's own volatility. The existing volatility_quantile/window params only add a selection gate on top of the absolute bar and have already been shown to over-prune (quantile 0.65 → 4 trades in the leanest year); they cannot re-express the threshold itself. This needs the threshold's units changed in code, which is why I queue it rather than pretend a genome captures it. Genome below is the incumbent as the paired baseline the coded rule must be measured against.
